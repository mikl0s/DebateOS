schema: 1
id: OM-095
status: required
name: Plymouth Boot Splash Theme
category: theming
intent: Install the Omarchy custom Plymouth boot splash theme and set it as the active default theme so the Omarchy branded
  animation appears during boot.
install_phase: login
depends_on:
- id: OM-016
ordering:
  before:
  - id: OM-099
translator_capabilities:
- copy-plymouth-theme-directory
- set-default-plymouth-theme
file_assets:
- src: plymouth-themes/omarchy/
  dst: /usr/share/plymouth/themes/omarchy/
