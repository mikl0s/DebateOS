schema: 1
id: OM-069
status: required
name: Vulkan Graphics Drivers
category: hardware-conditional
intent: 'Install Vulkan graphics drivers matching detected GPU hardware: Intel, AMD, or Apple Silicon. Multiple drivers may
  be installed if multiple GPUs are present.'
install_phase: config
depends_on:
- id: OM-001
ordering:
  after:
  - id: OM-068
hardware_condition:
  type: or
  operands:
  - type: leaf
    predicate: hw-pci-vendor
    values:
    - '8086'
  - type: leaf
    predicate: hw-pci-vendor
    values:
    - '1002'
  - type: leaf
    predicate: hw-pci-vendor
    values:
    - 106b
translator_capabilities:
- detect-gpu-vendors-from-pci-device-list
- install-vendor-specific-vulkan-driver-packages
packages:
- vulkan-intel
- vulkan-radeon
