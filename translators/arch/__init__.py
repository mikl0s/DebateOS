# translators/arch — Arch Linux translator package for DebateOS
# Consumes a ResolvedSpeech JSON (Phase 1 resolver output) and emits
# a bootable, fully-unattended Arch installer ISO via mkarchiso + Docker.
