---
phase: 01-schema-resolver-core
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - resolver/graph/graph.go
  - resolver/graph/toposort.go
  - resolver/graph/graph_test.go
  - resolver/graph/testdata/three-hop.yaml
  - resolver/graph/testdata/cycle.yaml
  - resolver/graph/testdata/cross-phase.yaml
autonomous: true
requirements: [RSLV-03]
must_haves:
  truths:
    - "Ordering constraints produce a deterministic topological install order"
    - "A circular ordering constraint produces a hard error that names the offending opinions"
    - "Toposort tie-breaking is lexicographic and reproducible regardless of input order"
    - "Cross-phase before/after edges override phase enum ordering (OM-023 case)"
  artifacts:
    - path: "resolver/graph/graph.go"
      provides: "BuildGraph from opinions producing dependency + ordering edges"
      exports: ["BuildGraph", "Graph"]
    - path: "resolver/graph/toposort.go"
      provides: "Kahn toposort with heap lexicographic tie-break + cycle extraction"
      exports: ["TopoSort"]
  key_links:
    - from: "resolver/graph/toposort.go"
      to: "container/heap"
      via: "min-heap of OpinionID for deterministic tie-breaking"
      pattern: "heap\\.(Init|Push|Pop)"
    - from: "resolver/graph/graph.go"
      to: "resolver (types.go)"
      via: "consumes Opinion/OpinionID shared types"
      pattern: "resolver\\.(Opinion|OpinionID)"
---

<objective>
Build resolver/graph: construction of the dependency + ordering graph from parsed opinions, and a
deterministic Kahn's-algorithm topological sort with lexicographic tie-breaking and cycle detection
that names the offending opinions. This is one of the two independent Wave 2 packages (parallel with
01-03 hardware/patch); both consume the shared types from 01-01 and have zero file overlap with each
other. resolver/resolve (01-04) consumes TopoSort output.

Per D19 (TDD): toposort + cycle detection are built test-first, encoding EC-035 (three-hop order),
EC-036 (cycle error), and the SR-006 cross-phase ordering exception (OM-023) before implementation.

Covers RSLV-03 (ordering constraints → toposort install order; cycles are hard composition-time errors
naming offending opinions).

Purpose: The resolve engine needs a correct, deterministic install order and a reliable cycle error;
isolating this in graph/ keeps the algorithm testable independent of the docs/04 rule engine.
Output: resolver/graph/ package with BuildGraph, TopoSort, and green tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-resolver-core/01-CONTEXT.md
@.planning/phases/01-schema-resolver-core/01-RESEARCH.md
@research/resolver-edge-cases.md
@research/schema-requirements.md
@docs/04-conflict-resolution.md
@.planning/phases/01-schema-resolver-core/01-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Consumed by 01-04 (resolve):
- **Package `resolver/graph`:**
  - `type Graph struct { ... }` — directed graph keyed by `resolver.OpinionID`, holding dependency edges (depends_on, SR-002) and ordering edges (ordering before/after + cross-phase, SR-006)
  - `func BuildGraph(opinions []resolver.Opinion) (*Graph, error)` — assembles edges; phase enum is a tie-break key, NOT a hard partition (cross-phase edges allowed per SR-006/OM-023)
  - `func (g *Graph) TopoSort() (order []resolver.OpinionID, cycle []resolver.OpinionID, err error)` — Kahn + container/heap lexicographic tie-break; on cycle returns sorted offending IDs and a non-nil error
  - Alternatively `func TopoSort(graph map[resolver.OpinionID][]resolver.OpinionID) (order, cycle []resolver.OpinionID, err error)` if a free function is cleaner (Claude's Discretion on exact signature; must be deterministic).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Graph + toposort RED tests (EC-035, EC-036, cross-phase)</name>
  <files>resolver/graph/graph_test.go, resolver/graph/testdata/three-hop.yaml, resolver/graph/testdata/cycle.yaml, resolver/graph/testdata/cross-phase.yaml</files>
  <read_first>
    - research/resolver-edge-cases.md — EC-035 (three-hop OM-009→OM-041→OM-023), EC-036 (docker/docker-dns cycle)
    - research/schema-requirements.md — SR-002 (dependencies), SR-006 (phase ordering + cross-phase OM-023 exception)
    - .planning/phases/01-schema-resolver-core/01-RESEARCH.md — Pattern 2 (Kahn + heap), Pitfall 1 (map iteration order), Pitfall 5 (cross-phase ordering)
    - resolver/types.go — Opinion/OpinionID and the ordering/install_phase fields
  </read_first>
  <behavior>
    - Test TestTopoSort/EC-035: graph with edges OM-009 before OM-041 before OM-023 yields order exactly [OM-009, OM-041, OM-023], nil error.
    - Test TestTopoSort/EC-036: graph with docker-service ↔ docker-dns mutual must-install-after returns a non-nil error and a cycle slice containing both offending opinion IDs (sorted, deterministic message).
    - Test TestTopoSortDeterministic: the same graph fed in shuffled input order produces byte-identical order output across ≥3 runs (lexicographic tie-break; guards Pitfall 1).
    - Test TestTopoSortCrossPhase: an opinion declared in the packaging phase with an explicit `after: [config-phase-opinion]` edge sorts AFTER that config-phase opinion (SR-006 OM-023 cross-phase override — phase enum does not override the explicit edge).
  </behavior>
  <action>
    Write resolver/graph/graph_test.go and the three testdata YAML fixtures FIRST. Fixtures are minimal
    opinions (parsed via resolver/parse or constructed directly as resolver.Opinion values) exercising the
    behaviors above; encode the EC ID in each subtest name (t.Run("EC-035", ...)). Commit RED.
  </action>
  <verify>
    <automated>cd /home/mikkel/repos/DebateOS && ! go test ./resolver/graph/ -run TestTopoSort -count=1 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Before implementation: `go test ./resolver/graph/ -run TestTopoSort` fails (RED — compile error or assertion failure)
    - Test names include the literal strings `EC-035` and `EC-036`
    - Git history shows this RED commit before Task 2's GREEN commit
  </acceptance_criteria>
  <done>Failing toposort/cycle/determinism/cross-phase tests committed RED with EC IDs in names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BuildGraph + TopoSort implementation (GREEN)</name>
  <files>resolver/graph/graph.go, resolver/graph/toposort.go</files>
  <read_first>
    - .planning/phases/01-schema-resolver-core/01-RESEARCH.md — Pattern 2 (full Kahn + stringHeap code), Pitfall 5 (cross-phase edges precede phase order)
    - resolver/graph/graph_test.go (Task 1) — the contract to satisfy
  </read_first>
  <action>
    Implement resolver/graph/graph.go: BuildGraph([]resolver.Opinion) assembles a directed graph from
    depends_on (SR-002) and ordering before/after (SR-006) edges. Treat install_phase as an additional
    deterministic tie-break key only — explicit before/after edges (including cross-phase, OM-023) take
    precedence and are never overridden by phase enum order. Implement resolver/graph/toposort.go:
    TopoSort using container/heap (min-heap keyed on OpinionID string for lexicographic tie-breaking,
    O(E log V)); on incomplete order, extract all nodes with remaining in-degree > 0, sort them, and return
    them as the cycle slice with a non-nil error naming them (deterministic message). No range-over-map in
    any output-producing path (collect keys, sort, iterate — Pitfall 1).
  </action>
  <verify>
    <automated>cd /home/mikkel/repos/DebateOS && go test ./resolver/graph/ -count=1 && go test ./resolver/graph/ -run TestTopoSortDeterministic -count=5 && go vet ./resolver/graph/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./resolver/graph/ -count=1` passes (GREEN, all Task 1 behaviors)
    - `go test ./resolver/graph/ -run TestTopoSortDeterministic -count=5` passes (stable across repeated runs)
    - `grep -q 'container/heap' resolver/graph/toposort.go`
    - No `for .* range` over a map writing to ordered output (manual review + determinism test guards this)
    - `go vet ./resolver/graph/` exits 0
  </acceptance_criteria>
  <done>BuildGraph + deterministic Kahn TopoSort with lexicographic tie-break and cycle-naming error; EC-035/EC-036/cross-phase/determinism tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parsed opinions → graph | Already-validated typed structs (validation happened in 01-01 parse); graph operates on trusted in-memory data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | DoS | toposort on adversarial graph | accept | O(E log V) bounded by opinion count; speeches are small (tens–hundreds of opinions); no untrusted unbounded recursion |
| T-01-06 | Tampering | non-deterministic cycle message leaking map order | mitigate | cycle slice sorted before return; determinism test enforces stable output (Pitfall 1) |
</threat_model>

<verification>
- `go test ./resolver/graph/ -count=1` green
- Determinism test stable across `-count=5`
- `go vet ./resolver/graph/` clean
</verification>

<success_criteria>
- BuildGraph constructs dependency + ordering graph from shared types
- TopoSort is deterministic (lexicographic), cross-phase aware (SR-006), and names cycle opinions
- EC-035 and EC-036 encoded as named tests, RED before GREEN (D19)
- RSLV-03 satisfied
</success_criteria>

<output>
Create `.planning/phases/01-schema-resolver-core/01-02-SUMMARY.md` when done.
Record the exact TopoSort signature for 01-04 to consume.
</output>
