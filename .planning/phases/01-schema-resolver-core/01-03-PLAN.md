---
phase: 01-schema-resolver-core
plan: 03
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - resolver/hardware/eval.go
  - resolver/hardware/hardware_test.go
  - resolver/hardware/testdata/profile-amd.yaml
  - resolver/hardware/testdata/profile-t2.yaml
  - resolver/patch/patch.go
  - resolver/patch/patch_test.go
  - resolver/patch/testdata/dracut-bridge.yaml
autonomous: true
requirements: [RSLV-02, RSLV-04]
must_haves:
  truths:
    - "A compound hardware predicate (AND/OR/NOT/set-membership/string-match) evaluates against a declared hardware profile"
    - "A hardware-conditional opinion is skipped when its condition is false and applied when true"
    - "A patch opinion attached to a conflict pair is discovered and offered automatically"
  artifacts:
    - path: "resolver/hardware/eval.go"
      provides: "EvalCondition for compound hardware predicates (SR-004/SR-005)"
      exports: ["EvalCondition", "HardwareProfile"]
    - path: "resolver/patch/patch.go"
      provides: "FindPatch discovering patch opinions for a conflict pair (SR-003/RSLV-02)"
      exports: ["FindPatch", "PatchOffer"]
  key_links:
    - from: "resolver/hardware/eval.go"
      to: "resolver (types.go HardwareExpr)"
      via: "recursive evaluation of the discriminated-union predicate"
      pattern: "resolver\\.HardwareExpr"
    - from: "resolver/patch/patch.go"
      to: "resolver (types.go PatchRef/known_patches)"
      via: "lookup patch opinions referenced by conflicting opinions"
      pattern: "known_patches|PatchRef|FindPatch"
---

<objective>
Build the two independent helper packages the resolve engine depends on: resolver/hardware (compound
hardware predicate evaluation against a declared hardware profile, SR-004/SR-005) and resolver/patch
(first-class patch-opinion discovery for conflict pairs, SR-003/RSLV-02). This is the second Wave 2
plan, parallel with 01-02 (graph) — zero file overlap with it. Both consume 01-01 shared types.
resolver/resolve (01-04) consumes EvalCondition and FindPatch.

Per D19 (TDD): both packages built test-first. Hardware encodes EC-037 (NVIDIA skip, condition false)
and EC-038 (Apple T2 apply, condition true). Patch encodes EC-032-style discovery (patch found for a
conflict pair).

Covers RSLV-04 (hardware-conditional evaluation with skip/apply) and RSLV-02 (patch opinions discovered
and offered automatically).

Purpose: Hardware evaluation and patch lookup are orthogonal concerns isolated from the docs/04 rule
engine so each is independently testable; resolve composes them.
Output: resolver/hardware/ and resolver/patch/ packages with green tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-resolver-core/01-CONTEXT.md
@.planning/phases/01-schema-resolver-core/01-RESEARCH.md
@research/resolver-edge-cases.md
@research/schema-requirements.md
@docs/04-conflict-resolution.md
@.planning/phases/01-schema-resolver-core/01-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Consumed by 01-04 (resolve):
- **Package `resolver/hardware`:**
  - `type HardwareProfile struct { ... }` — declared hardware: gpu, cpu_features, pci IDs, dmi/product matches, battery_present, named predicates (sufficient to evaluate OM-037/068/071/074/077/086/088 conditions and EC-037/EC-038 fixtures)
  - `func EvalCondition(expr resolver.HardwareExpr, hw HardwareProfile) (bool, error)` — recursively evaluates leaf/and/or/not + set-membership + string-match
- **Package `resolver/patch`:**
  - `type PatchOffer struct { PatchID resolver.OpinionID; Pair [2]resolver.OpinionID; ... }`
  - `func FindPatch(a, b resolver.OpinionID, opinions []resolver.Opinion) *PatchOffer` — returns the patch opinion attached to the (a,b) conflict pair via known_patches metadata, or nil
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hardware predicate evaluation (EC-037 skip, EC-038 apply) RED→GREEN</name>
  <files>resolver/hardware/hardware_test.go, resolver/hardware/eval.go, resolver/hardware/testdata/profile-amd.yaml, resolver/hardware/testdata/profile-t2.yaml</files>
  <read_first>
    - research/resolver-edge-cases.md — EC-037 (NVIDIA skipped, amd-radeon profile), EC-038 (Apple T2 applied, PCI 106b:1801)
    - research/schema-requirements.md — SR-004 (single predicate), SR-005 (compound AND/OR/NOT/set-membership/string-match; OM-071/074/077/086/088 examples)
    - .planning/phases/01-schema-resolver-core/01-RESEARCH.md — Pitfall 3 (compound predicate underspecification)
    - resolver/types.go — HardwareExpr discriminated-union shape
  </read_first>
  <behavior>
    - Test TestHardwareEval/EC-037: EvalCondition for an NVIDIA-GPU leaf predicate against profile-amd.yaml (gpu amd-radeon-rx-7600, no NVIDIA) returns false → opinion is skipped.
    - Test TestHardwareEval/EC-038: EvalCondition for a PCI set-membership predicate (106b:1801 OR 106b:1802) against profile-t2.yaml (pci includes 106b:1801) returns true → opinion is applied.
    - Test TestHardwareEvalCompound: an AND-of(intel, battery_present, NOT match "XPS") expression evaluates correctly for both matching and non-matching profiles (SR-005 OM-077 shape).
    - Test TestHardwareEvalSetMembership: a cpu-model set-membership leaf returns true only when the declared model is in the set (OM-071 shape).
  </behavior>
  <action>
    Write resolver/hardware/hardware_test.go and the two profile fixtures FIRST; encode EC-037/EC-038 in
    subtest names. Commit RED. Then implement resolver/hardware/eval.go: HardwareProfile struct and
    EvalCondition recursively walking resolver.HardwareExpr — leaf (named predicate / set-membership /
    string-match), and/or/not combinators. Return (bool, error); error only on malformed expr (unknown
    Type). No float fields; deterministic.
  </action>
  <verify>
    <automated>cd /home/mikkel/repos/DebateOS && go test ./resolver/hardware/ -count=1 && go vet ./resolver/hardware/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./resolver/hardware/ -count=1` passes (GREEN)
    - Subtest names contain `EC-037` and `EC-038`
    - `go vet ./resolver/hardware/` exits 0
    - RED commit precedes GREEN commit in git history for this task
  </acceptance_criteria>
  <done>EvalCondition handles leaf/and/or/not/set-membership/string-match; EC-037 skip and EC-038 apply green after RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Patch opinion discovery (RSLV-02) RED→GREEN</name>
  <files>resolver/patch/patch_test.go, resolver/patch/patch.go, resolver/patch/testdata/dracut-bridge.yaml</files>
  <read_first>
    - research/resolver-edge-cases.md — EC-032 (dracut-omarchy-bridge patch for mkinitcpio↔dracut), EC-034 (merged-pacman-conf patch)
    - research/schema-requirements.md — SR-003 (conflict declarations + known_patches references)
    - docs/04-conflict-resolution.md — "Patch opinions (first-class)": discoverable, attached to the conflict pair
    - resolver/types.go — PatchRef / known_patches field
  </read_first>
  <behavior>
    - Test TestPatchDiscovery/EC-032: given a conflict pair (mkinitcpio opinion, dracut opinion) where one declares known_patches referencing dracut-omarchy-bridge, FindPatch returns a non-nil PatchOffer whose PatchID is that patch and whose Pair contains both conflicting IDs.
    - Test TestPatchDiscoveryNoPatch: a conflict pair with no attached patch returns nil.
    - Test TestPatchDiscoverySymmetric: FindPatch(a,b,...) and FindPatch(b,a,...) return the same offer (pair order-independent).
  </behavior>
  <action>
    Write resolver/patch/patch_test.go and the dracut-bridge.yaml fixture FIRST; encode EC-032 in subtest
    name. Commit RED. Then implement resolver/patch/patch.go: PatchOffer struct and
    FindPatch(a, b resolver.OpinionID, opinions []resolver.Opinion) *PatchOffer that scans known_patches /
    conflict metadata on both opinions, matches a patch opinion (category patch) whose target pair is {a,b},
    and returns it order-independently; nil when none. Keep lookup deterministic (sort candidates by ID).
  </action>
  <verify>
    <automated>cd /home/mikkel/repos/DebateOS && go test ./resolver/patch/ -count=1 && go vet ./resolver/patch/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./resolver/patch/ -count=1` passes (GREEN)
    - Subtest name contains `EC-032`
    - FindPatch is symmetric (TestPatchDiscoverySymmetric green)
    - `go vet ./resolver/patch/` exits 0
    - RED commit precedes GREEN commit
  </acceptance_criteria>
  <done>FindPatch discovers patch opinions for a conflict pair order-independently; EC-032 + no-patch + symmetry tests green after RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Declared hardware profile → eval | User-declared hardware (data); no scanning in Phase 1; bounded recursion over HardwareExpr |
| known_patches references → patch lookup | Opinion-declared patch references (data only; patches are not executed in Phase 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | DoS | EvalCondition recursion on deeply nested HardwareExpr | mitigate | expr already JSON-Schema-validated in 01-01 parse; recursion bounded by validated document depth |
| T-01-08 | Tampering | malformed HardwareExpr Type | mitigate | EvalCondition returns error on unknown Type rather than defaulting silently |
| T-01-09 | Spoofing | patch falsely claiming to resolve a pair | accept | Phase 1 trusts opinion metadata as data; patch application/correctness is composition-review responsibility (explanation surfaced in 01-04) |
</threat_model>

<verification>
- `go test ./resolver/hardware/ ./resolver/patch/ -count=1` green
- `go vet ./resolver/hardware/ ./resolver/patch/` clean
</verification>

<success_criteria>
- EvalCondition evaluates compound predicates (SR-004/SR-005); EC-037 skip + EC-038 apply green
- FindPatch discovers patches for conflict pairs order-independently; EC-032 green
- All RED-before-GREEN per D19
- RSLV-02 and RSLV-04 satisfied
</success_criteria>

<output>
Create `.planning/phases/01-schema-resolver-core/01-03-SUMMARY.md` when done.
Record EvalCondition and FindPatch signatures + HardwareProfile shape for 01-04.
</output>
