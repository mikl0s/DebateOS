---
phase: 04-debian-translator
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - translators/common/__init__.py
  - translators/common/contract.py
  - translators/common/manifest.py
  - translators/common/firstrun.py
  - translators/common/templates/firstrun.service.tpl
  - translators/common/tests/__init__.py
  - translators/common/tests/test_common_shared.py
  - translators/common/pytest.ini
  - translators/arch/contract.py
  - translators/arch/manifest.py
  - translators/arch/firstrun.py
autonomous: true
requirements: [DEB-03]
must_haves:
  truths:
    - "Foundation-neutral translator logic (contract loaders, BuildManifest, first-run unit rendering) lives in translators/common/ and is importable by both arch and debian"
    - "The Arch translator continues to pass all of its existing tests after the extraction (no regression)"
    - "examples/omarchy still resolves and translates clean after the extraction"
    - "If clean extraction is not achievable within the clean-import bar, the duplication is documented in-code and the shared modules are left translator-local (fallback path)"
  artifacts:
    - path: "translators/common/__init__.py"
      provides: "Shared translator package marker"
    - path: "translators/common/contract.py"
      provides: "load_resolved_speech, load_opinion_bodies (foundation-neutral)"
      contains: "def load_opinion_bodies"
    - path: "translators/common/manifest.py"
      provides: "BuildManifest, derive_source_date_epoch (foundation-neutral)"
      contains: "class BuildManifest"
    - path: "translators/common/firstrun.py"
      provides: "render_firstrun_unit, firstrun_unit_name (parameterized template path)"
      contains: "def render_firstrun_unit"
    - path: "translators/common/tests/test_common_shared.py"
      provides: "Direct unit tests for the shared modules"
  key_links:
    - from: "translators/arch/generator.py"
      to: "translators/common contract/manifest"
      via: "import (direct or shim re-export)"
      pattern: "contract|manifest"
    - from: "translators/arch/profile.py"
      to: "translators/common.firstrun"
      via: "render_firstrun_unit import"
      pattern: "render_firstrun_unit"
---

<objective>
Extract the byte-identical, foundation-neutral pieces of the Arch translator into
`translators/common/` so the Debian translator (Plan 04-03) imports them instead of
copy-pasting. This is the first half of the DEB-03 leak fix: the shared core proves it
is genuinely foundation-neutral by serving two foundations.

The three extraction candidates (confirmed byte/near-identical in 04-RESEARCH.md
§Common/Shared Code Recommendation):
- `contract.py`   — load_resolved_speech, load_opinion_bodies (no Arch-specific code)
- `manifest.py`   — BuildManifest, derive_source_date_epoch (purely foundation-neutral)
- `firstrun.py`   — render_firstrun_unit + helpers (parameterize the template path)

Hard constraint (regression gate, CONTEXT D / 04-RESEARCH.md): the Arch translator and
examples/omarchy MUST stay green after the move. The bare-name imports in arch
(`from contract import ...`, `from manifest import ...`, `from firstrun import ...`) must
keep working.

Open-question resolution (04-RESEARCH.md §Open Questions RESOLVED, item 1): attempt the
extraction in Wave 1; if it exceeds the clean-import bar (see Task 3 fallback), fall back
to documented duplication and leave the modules translator-local. Do NOT let extraction
block Plan 04-03.

Purpose: Earn invariant 1 ("translators own mechanics") — the shared core is the proof the
abstraction is real, not Arch-shaped. Cited per D-DEB-03 leak audit (build.go is the other half, Plan 04-04).
Output: translators/common/ package + passing shared tests + green Arch regression.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-debian-translator/04-RESEARCH.md

# The exact modules being extracted (read in full — they move verbatim):
@translators/arch/contract.py
@translators/arch/manifest.py
@translators/arch/firstrun.py
@translators/arch/generator.py
@translators/arch/profile.py
@translators/arch/pytest.ini
@translators/arch/templates/firstrun.service.tpl
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — shared-module tests in translators/common/tests/</name>
  <read_first>
    - translators/arch/tests/test_manifest.py (the existing assertions to mirror against the shared module)
    - translators/arch/tests/test_firstrun.py
    - translators/arch/contract.py, manifest.py, firstrun.py (the surface under test)
    - translators/arch/pytest.ini (pythonpath = . / testpaths = tests pattern)
  </read_first>
  <behavior>
    - Test: `from common.contract import load_opinion_bodies` loads a JSON-array fixture and a directory fixture into an id→dict index (mirror arch contract tests).
    - Test: `from common.manifest import derive_source_date_epoch` is deterministic and clamps to [1577836800, 2208988800) for the same bytes.
    - Test: `from common.manifest import BuildManifest` .from_resolved aggregates target_packages (deduped, install_order order), system_services vs deferred_services split, first_run collection, sysctl_params, custom_repos + trust_warnings for sig_level Never/OptionalTrustAll.
    - Test: `from common.firstrun import render_firstrun_unit, firstrun_unit_name` renders a unit containing the Description, ConditionPathExists flag-file guard, ExecStart, and ExecStartPost touch; firstrun_unit_name returns `debateos-firstrun-<id>.service`.
    - Test: render_firstrun_unit accepts a template_dir/template_path parameter (parameterized so debian can pass its own path) AND defaults to the common template when omitted.
  </behavior>
  <action>
    Create translators/common/pytest.ini (testpaths = tests, pythonpath = ..) and
    translators/common/tests/__init__.py + translators/common/tests/test_common_shared.py.
    Tests import from the `common` package (the imports will FAIL now — modules do not exist
    yet: this is RED). Add a small fixtures inline (resolved dict + opinions index built in
    Python, not external files where avoidable) so the test is self-contained. Mirror the
    assertion style of translators/arch/tests/test_manifest.py and test_firstrun.py exactly so
    behavior parity is guaranteed.
    Commit message: `test(04-01): RED shared common-module tests`
  </action>
  <verify>
    <automated>! python3 -m pytest translators/common/tests/ -q 2>/dev/null</automated>
  </verify>
  <done>translators/common/tests/test_common_shared.py exists; pytest fails with import/collection errors because translators/common/ modules do not exist yet (RED proven).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — create translators/common/ and re-point Arch imports</name>
  <read_first>
    - translators/arch/contract.py (moves verbatim)
    - translators/arch/manifest.py (moves verbatim; note `from capabilities import check_capabilities` — capabilities stays translator-local, see action)
    - translators/arch/firstrun.py (moves; the only change is parameterizing _TEMPLATES_DIR)
    - translators/arch/generator.py lines 36-40 (the bare imports to keep working)
    - translators/arch/profile.py line 38 (from firstrun import ...)
  </read_first>
  <behavior>
    - common.contract, common.manifest, common.firstrun importable and pass Task 1 tests.
    - Arch's bare imports (`from contract import ...`, `from manifest import ...`, `from firstrun import ...`) still resolve.
  </behavior>
  <action>
    Create translators/common/__init__.py, contract.py (verbatim from arch/contract.py),
    manifest.py, firstrun.py, and translators/common/templates/firstrun.service.tpl
    (copy of arch template).

    CRITICAL — manifest.py dependency: arch/manifest.py does `from capabilities import
    check_capabilities`. The capability gate is translator-LOCAL (Debian declares a different
    capability set), so it must NOT move to common. Refactor BuildManifest.from_resolved to
    accept the gate as already-run by the caller: REMOVE the `check_capabilities` import and
    the in-method `check_capabilities(...)` call from the common copy; document in the
    docstring that the CALLER (generator.py) runs the capability gate BEFORE from_resolved.
    Then update arch/generator.py to call check_capabilities() explicitly before
    BuildManifest.from_resolved (it currently relies on from_resolved calling it). This keeps
    the gate behavior identical (still fires before assembly) but makes the manifest module
    foundation-neutral.

    firstrun.py: parameterize the template directory. Add a `template_dir: str = None` param to
    render_firstrun_unit (and a module default pointing at translators/common/templates/). When
    None, use the common default. This lets the Debian translator pass its own template dir if
    it ever diverges.

    Re-point Arch to common WITHOUT breaking bare imports. Use the shim approach: replace the
    BODY of translators/arch/contract.py, manifest.py, firstrun.py with a re-export shim, e.g.
    `from common.contract import *  # noqa: F401,F403` plus explicit re-exports of the public
    names. The shim makes `from contract import load_opinion_bodies` continue to resolve to the
    common implementation while keeping a single source of truth. Ensure the `common` package is
    importable from arch's pythonpath: arch/generator.py inserts _ARCH_DIR on sys.path; add a
    sibling insert for translators/ (parent of arch) so `import common` resolves. arch/pytest.ini
    `pythonpath = .` runs from translators/arch — add `..` so `common` is importable in arch tests too.

    Commit message: `feat(04-01): extract translators/common shared core; arch re-exports it`
  </action>
  <verify>
    <automated>python3 -m pytest translators/common/tests/ -q && python3 -m pytest translators/arch/tests/ -q</automated>
  </verify>
  <done>common tests pass; ALL arch tests still pass (regression green). Arch generator.py runs the capability gate explicitly before BuildManifest.from_resolved.</done>
</task>

<task type="auto">
  <name>Task 3: Regression gate + 30-min fallback decision</name>
  <read_first>
    - examples/omarchy_test.go (the Go north-star clean-resolve test)
    - scripts/arch-northstar-check.sh (the --skip-build equivalence gate)
  </read_first>
  <action>
    Run the full regression set to prove the extraction did not break Arch or the examples:
    `go test ./... -count=1` and `python3 -m pytest translators/arch/tests/ translators/common/tests/ -q`
    and `bash scripts/arch-northstar-check.sh --skip-build`.

    FALLBACK RULE (04-RESEARCH.md Open-Question item 1 — the clean-import bar): if at this point
    the Arch tests or the north-star gate cannot be made green via the shim approach within a
    reasonable effort (broken bare imports that resist the shim, pythonpath conflicts, or the
    north-star gate regresses), DO NOT force it. Instead: (a) revert translators/arch/* to its
    pre-extraction state, (b) keep translators/common/ as a NEW package that the Debian translator
    (04-03) will import directly (arch keeps its own copies), (c) add a `# SHARED: see
    translators/common — duplication documented per 04-RESEARCH.md` comment at the top of each of
    arch/contract.py, manifest.py, firstrun.py, and (d) record the fallback in the plan SUMMARY
    Deviations section with the specific blocker. The common/ package + its passing tests are the
    deliverable either way; the arch re-point is the nice-to-have.
    Commit message: `test(04-01): regression gate green after common extraction`
  </action>
  <verify>
    <automated>go test ./... -count=1 && python3 -m pytest translators/arch/tests/ translators/common/tests/ -q && bash scripts/arch-northstar-check.sh --skip-build</automated>
  </verify>
  <done>go suite green; arch + common pytest green; north-star --skip-build gate passes. If the fallback path was taken, arch/* modules carry the SHARED duplication comment and the SUMMARY documents the blocker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| opinion data → manifest | Opinion YAML/JSON (curator-authored) parsed into BuildManifest; untrusted strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | common/manifest.py trust_warnings | mitigate | Preserve the sig_level Never/OptionalTrustAll trust-warning emission verbatim during the move; covered by a Task 1 test asserting the warning strings are produced |
| T-04-02 | Information Disclosure | extraction regression | accept | No new attack surface; pure refactor. Regression gate (Task 3) is the control |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan (pytest already approved, 04-RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
- `python3 -m pytest translators/common/tests/ -q` — shared modules behave correctly
- `python3 -m pytest translators/arch/tests/ -q` — Arch regression green
- `go test ./... -count=1` — examples/omarchy + resolver green
- `bash scripts/arch-northstar-check.sh --skip-build` — 16/16 north-star equivalence
</verification>

<success_criteria>
- translators/common/ contains contract.py, manifest.py, firstrun.py + template + tests
- common modules are foundation-neutral (no capabilities import in manifest; parameterized firstrun template)
- Arch translator + examples/omarchy stay fully green (or fallback documented per Task 3)
- DEB-03 leak-audit groundwork: the shared core is proven reusable by a second foundation
</success_criteria>

<output>
Create `.planning/phases/04-debian-translator/04-01-SUMMARY.md` when done.
Record whether the clean extraction succeeded or the fallback (documented duplication) was taken.
</output>
