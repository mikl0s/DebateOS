---
phase: 04-debian-translator
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/dual-foundation/speech.yaml
  - examples/dual-foundation/points/DF-base-cli.yaml
  - examples/dual-foundation/points/DF-system-tuning.yaml
  - examples/dual-foundation/opinions/DF-001.yaml
  - examples/dual-foundation/opinions/DF-002.yaml
  - examples/dual-foundation/opinions/DF-003.yaml
  - examples/dual-foundation/opinions/DF-004.yaml
  - examples/dual-foundation/opinions/DF-005.yaml
  - examples/dual-foundation/assets/motd
  - examples/dual-foundation/README.md
  - examples/dual_foundation_test.go
autonomous: true
requirements: [DEB-02]
must_haves:
  truths:
    - "A foundation-neutral representative speech exists in examples/dual-foundation/ authored fresh (not Omarchy)"
    - "Every REQUIRED opinion in the speech uses only capability tokens that BOTH the Arch and (future) Debian translators can declare"
    - "The speech resolves clean (no hard conflicts, non-empty install order) via a Go test mirroring the omarchy harness"
    - "The opinions cover the full manifest-aggregation surface: packages, file assets, a service, a sysctl drop-in, a group membership"
  artifacts:
    - path: "examples/dual-foundation/speech.yaml"
      provides: "Representative dual-foundation composition (foundation: debian)"
      contains: "foundation"
    - path: "examples/dual-foundation/opinions/DF-001.yaml"
      provides: "First foundation-neutral opinion (package install)"
      contains: "id: DF-001"
    - path: "examples/dual_foundation_test.go"
      provides: "TestExampleDualFoundation clean-resolve gate"
      exports: ["TestExampleDualFoundation"]
  key_links:
    - from: "examples/dual-foundation/speech.yaml"
      to: "examples/dual-foundation/points/*.yaml"
      via: "point references"
      pattern: "points:"
    - from: "examples/dual_foundation_test.go"
      to: "resolver/resolve.Resolve"
      via: "clean-resolution assertion"
      pattern: "resolve\\.Resolve"
---

<objective>
Author the foundation-neutral representative speech that is the heart of the dual-foundation
proof (DEB-02). It is authored FRESH — NOT Omarchy (Omarchy is Arch-specific by nature;
CONTEXT.md <domain> + <specifics> are explicit on this).

Per the resolved open question (04-RESEARCH.md §Open Questions RESOLVED, item 2) and CONTEXT
"Claude's Discretion": 5 opinions across these genuinely OS-agnostic categories so BOTH
translators can effectuate every REQUIRED one (per D-DEB-02):
- install-packages (a base CLI toolset)
- deploy-config-file-tree (a config file asset)
- enable-systemd-service (a service)
- write-sysctl-drop-in (a sysctl param)
- add-user-to-group (a group membership)

These exact categories exercise the full BuildManifest aggregation pipeline WITHOUT any
foundation-specific mechanic (NO mkinitcpio, NO limine, NO pacman-AUR, NO initramfs-tools
tokens — see 04-RESEARCH.md Pitfall 5 + Arch-Leak Audit Findings 3/4). The capability tokens
used MUST be a subset of what the Debian translator declares in Plan 04-03's capabilities.json
AND what the Arch translator already declares.

Purpose: The same resolved.json from THIS speech feeds both translators in Plan 04-05's
dual-foundation-check.sh. Cited per D-DEB-02 (dual-foundation proof).
Output: examples/dual-foundation/ (speech + 2 points + 5 opinions + asset + README) + a Go clean-resolve gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-debian-translator/04-RESEARCH.md

# The schema + example patterns to mirror (read for exact field shapes):
@examples/omarchy/opinions/OM-001.yaml
@examples/omarchy_test.go
@examples/two-point-clean/speech.yaml
@translators/arch/capabilities.json
@schemas/opinion.schema.json
@schemas/speech.schema.json
@schemas/point.schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 5 foundation-neutral opinions + 2 points + speech + asset</name>
  <read_first>
    - examples/omarchy/opinions/OM-001.yaml (opinion YAML shape: schema/id/status/name/category/intent/install_phase/translator_capabilities + payload fields)
    - examples/two-point-clean/speech.yaml AND examples/omarchy/speech.yaml (speech shape: schema/foundation/points)
    - examples/omarchy/points/*.yaml (point shape: id/name/members)
    - schemas/opinion.schema.json (authoritative field names + enums: status required|nice-to-have, install_phase enum, file_assets {src,dst}, services, sysctl_params {key,value,drop_in_file}, group_memberships {group})
    - translators/arch/capabilities.json (the exact token strings Arch declares — pick foundation-neutral tokens from here: install-packages, deploy-config-file-tree, add-user-to-group, and the sysctl/service equivalents)
  </read_first>
  <action>
    Create examples/dual-foundation/ with:

    opinions/DF-001.yaml — status: required, category: package-install, install_phase: packaging,
      translator_capabilities: [install-packages]. packages: [git, curl, vim] (names valid in BOTH
      Arch and Debian repos — verify none are Arch-only). Intent text: a base CLI toolset.
    opinions/DF-002.yaml — status: required, category: config-file, install_phase: config,
      translator_capabilities: [deploy-config-file-tree]. file_assets: [{src: assets/motd, dst: etc/motd}]
      (RELATIVE dst — absolute paths are rejected by _sanitize_dst; see 02-05-SUMMARY deviation #2).
    opinions/DF-003.yaml — status: required, category: service, install_phase: config,
      translator_capabilities: [enable-systemd-service]. services: [{name: systemd-timesyncd.service, deferred: false}].
    opinions/DF-004.yaml — status: required, category: sysctl, install_phase: config,
      translator_capabilities: [write-sysctl-drop-in]. sysctl_params: [{key: net.ipv4.tcp_fastopen, value: "3", drop_in_file: 50-debateos.conf}].
    opinions/DF-005.yaml — status: required, category: user-group, install_phase: post-install,
      translator_capabilities: [add-user-to-group]. group_memberships: [{group: video}].

    Use ONLY capability tokens present in translators/arch/capabilities.json (so Arch effectuates them)
    AND foundation-neutral (so Debian 04-03 can declare them). If a chosen token is absent from arch
    capabilities.json, switch to the closest token that IS present. Each opinion: schema: 1, no
    dependencies/conflicts (keep the speech trivially clean-resolving), no hardware_condition (so it
    applies on a baseline profile).

    points/DF-base-cli.yaml — id: DF-base-cli, members: [DF-001, DF-002].
    points/DF-system-tuning.yaml — id: DF-system-tuning, members: [DF-003, DF-004, DF-005].

    speech.yaml — schema: 1, id + name, foundation: debian, points: [{id: DF-base-cli}, {id: DF-system-tuning}].
    (foundation: debian so the dual-foundation-check resolves once and routes to debian by default; the
    Arch translator is invoked on the SAME resolved.json explicitly in 04-05.)

    assets/motd — a small static text banner referenced by DF-002's file_asset src.

    README.md — explain this is the DEB-02 dual-foundation proof: one foundation-neutral speech, both
    translators effectuate every required opinion; list the 5 opinions and why each is OS-agnostic;
    note CC0 licensing (examples/LICENSE).
    Commit message: `feat(04-02): foundation-neutral dual-foundation example speech (DEB-02)`
  </action>
  <verify>
    <automated>test -f examples/dual-foundation/speech.yaml && test -f examples/dual-foundation/opinions/DF-005.yaml && test -f examples/dual-foundation/assets/motd && grep -q 'foundation: debian' examples/dual-foundation/speech.yaml && c=0; for f in examples/dual-foundation/opinions/DF-00*.yaml; do grep -qE 'translator_capabilities' "$f" && c=$((c+1)); done; test "$c" -eq 5</automated>
  </verify>
  <done>5 opinions + 2 points + speech.yaml (foundation: debian) + assets/motd + README exist; every opinion declares translator_capabilities; all dst paths relative.</done>
</task>

<task type="auto">
  <name>Task 2: TestExampleDualFoundation clean-resolve Go gate</name>
  <read_first>
    - examples/omarchy_test.go (assembleOpinionsFromSpeech, loadOmarchyPoints/Opinions, findRoot helpers, and the clean-resolution assertions to mirror — no hard conflict, non-empty install order, non-empty applied)
    - examples/examples_test.go (findRoot helper lives here; do not redefine it)
    - resolver/parse + resolver/resolve API (parse.ParseSpeech, parse.ParseOpinion, resolve.Resolve signature, ResolvedSpeech fields)
  </read_first>
  <action>
    Create examples/dual_foundation_test.go in package examples_test with TestExampleDualFoundation.
    Mirror examples/omarchy_test.go: load examples/dual-foundation/speech.yaml, points/*.yaml, and
    opinions/DF-*.yaml; assemble via the same point-expansion logic; resolve on an empty baseline
    HardwareProfile. Reuse findRoot from examples_test.go (same package — do not redeclare it).

    Assertions:
    - Resolve returns no error and a non-nil *ResolvedSpeech.
    - No explanation contains "Hard conflict".
    - len(InstallOrder) == 5 and len(Applied) == 5 (all 5 required opinions applied; nothing dropped/skipped).
    - len(Dropped) == 0 and len(Skipped) == 0.
    - Every applied opinion ID has the "DF-" prefix.
    Add a t.Logf summary line (Applied/Skipped/Dropped/InstallOrder counts) like the omarchy test.
    Commit message: `test(04-02): TestExampleDualFoundation clean-resolve gate (DEB-02)`
  </action>
  <verify>
    <automated>go test ./examples/ -run TestExampleDualFoundation -v -count=1</automated>
  </verify>
  <done>TestExampleDualFoundation passes: the dual-foundation speech resolves clean with all 5 opinions applied, zero dropped/skipped, zero hard conflicts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| opinion YAML → resolver | Curator-authored example opinions parsed by the resolver |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | DF-002 file_asset dst | mitigate | dst is a relative path (etc/motd); the translator's _sanitize_dst gate (reused from Arch, exercised in 04-03) rejects absolute/traversal. Authoring with relative dst prevents the 02-05 deviation #2 class of failure |
| T-04-04 | Information Disclosure | examples are public | accept | examples/ is CC0 public content by design; no secrets in the dual-foundation speech (PRIV-01 unaffected) |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this plan (04-RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
- File presence + foundation + capability-token checks (Task 1 automated)
- `go test ./examples/ -run TestExampleDualFoundation -v -count=1` — clean resolution gate
- `go test ./... -count=1` — no regression in the rest of the examples package
</verification>

<success_criteria>
- examples/dual-foundation/ is a complete, schema-valid, foundation-neutral composition authored fresh
- All 5 required opinions use only tokens both translators declare (no Arch-specific mechanics)
- TestExampleDualFoundation proves clean resolution (DEB-02 resolve-once foundation)
</success_criteria>

<output>
Create `.planning/phases/04-debian-translator/04-02-SUMMARY.md` when done.
List the 5 capability tokens used so Plan 04-03 can guarantee the Debian capabilities.json declares them all.
</output>
