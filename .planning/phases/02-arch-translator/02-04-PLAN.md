---
phase: 02-arch-translator
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/omarchy/opinions/
  - examples/omarchy/points/
  - examples/omarchy/speech.yaml
  - examples/omarchy/README.md
  - examples/omarchy/LICENSE
  - examples/omarchy_test.go
autonomous: true
requirements: [ARCH-02]
user_setup: []

must_haves:
  truths:
    - "All 134 OM-NNN opinions exist as schema-valid YAML under examples/omarchy/opinions/"
    - "All 32 evidence-derived points exist under examples/omarchy/points/, collectively referencing every OM-NNN exactly once"
    - "A speech.yaml targets foundation arch (vanilla-arch) and composes the 32 points"
    - "The Omarchy speech resolves with no hard conflicts and a non-empty install order"
    - "Status assignments follow the RESOLVED open question: OM-001/OM-006/OM-097/OM-099 + hardware-conditional opinions required; themes (OM-114..134) nice-to-have"
  artifacts:
    - path: "examples/omarchy/speech.yaml"
      provides: "The north-star composition targeting vanilla Arch"
      contains: "foundation"
    - path: "examples/omarchy_test.go"
      provides: "Go harness: parse + resolve examples/omarchy with no hard conflicts"
      min_lines: 40
    - path: "examples/omarchy/LICENSE"
      provides: "CC0 license for example content"
  key_links:
    - from: "examples/omarchy_test.go"
      to: "resolver/resolve.Resolve"
      via: "loads opinions + speech, asserts clean resolution"
      pattern: "resolve\\.Resolve"
    - from: "examples/omarchy/speech.yaml"
      to: "examples/omarchy/points/"
      via: "speech references points by id; points reference opinions by id"
      pattern: "points:"
---

<objective>
Author the Omarchy north-star composition (ARCH-02): all 134 OM-NNN opinions as schema-valid YAML, grouped into the 32 evidence-derived points, plus one speech targeting vanilla Arch — and a Go test harness that proves the speech parses and resolves with zero hard conflicts. Authoring is script-assisted from the Phase 0 inventory; every file must pass the existing resolver/parse validation.

Purpose: This is the content half of the north star. The translator (Plans 01/02) is meaningless without a real, resolvable Omarchy speech to translate. Producing clean-resolving content here de-risks the full north-star integration (Plan 05) and proves invariant 1 (OS-agnostic opinions, no Arch mechanics in content).

Output: `examples/omarchy/{opinions/*, points/*, speech.yaml, README.md, LICENSE}` + `examples/omarchy_test.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-arch-translator/02-CONTEXT.md
@.planning/phases/02-arch-translator/02-RESEARCH.md

# The 32 point groupings with authoritative OM-NNN membership (coverage rule: each OM exactly once):
@research/omarchy-points.md
# Existing example that already encodes 4 real OM opinions — copy its YAML shape exactly:
@examples/omarchy-mini/opinions.yaml
@examples/omarchy-mini/speech.yaml
# Opinion/Point/Speech field contract + json/yaml tags the YAML must match:
@resolver/types.go
# The existing Go harness pattern to extend (loadExample, findRoot, Resolve assertions):
@examples/examples_test.go
</context>

<artifacts_this_phase_produces>
- `examples/omarchy/` is the canonical north-star speech consumed by Plan 05's resolve→translate→build pipeline.
- Opinion bodies here are the OPINION-BODY SOURCE for the translator input contract (Plan 01 contract.load_opinion_bodies reads a directory of opinion YAML). The resolved.json (decisions) + this opinion directory together form the translator input.
- Status policy (RESOLVED OQ-1): required = OM-001 (repo), OM-006 (compositor), OM-097 (display manager), OM-099 (bootloader), and all hardware-conditional opinions (their hardware condition gates them); nice-to-have = themes OM-114..OM-134; remaining opinions follow their natural role from the inventory.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author all 134 opinions + 32 points (schema-valid)</name>
  <files>examples/omarchy/opinions/, examples/omarchy/points/, examples/omarchy/LICENSE, examples/omarchy/README.md</files>
  <read_first>
    - research/omarchy-points.md (32 points + exact OM-NNN membership lists + the Summary Statistics table for the coverage check)
    - research/omarchy-opinion-inventory.md (the authoritative per-opinion source: category, intent, packages, capabilities, ordering — script-assist authoring from this file)
    - examples/omarchy-mini/opinions.yaml (the exact YAML field shape: schema, id, name, category, intent, status, depends_on, packages, custom_repos, …)
    - resolver/types.go (every Opinion/Point field + yaml tags — opinions must be OS-agnostic: intent free of Arch mechanics per invariant 1; Arch packages ARE allowed in packages[] as that is the named-package payload)
  </read_first>
  <action>
    Script-assisted authoring: write a Python helper COMMITTED at examples/omarchy/gen/generate.py (CC0, reproducibility — acceptance: file exists and re-running it is idempotent on the committed YAML) that reads research/omarchy-opinion-inventory.md and emits one opinion YAML per OM-NNN into examples/omarchy/opinions/ (OM-001.yaml .. OM-134.yaml). Each opinion carries: schema:1, id, name, category, intent (OS-agnostic), status (per the status policy below), and the appropriate payload fields (packages, file_assets, services, sysctl_params, kernel_params, group_memberships, custom_repos, theme, script_payload+capabilities, execution_phase, depends_on, ordering, hardware_condition) drawn from the inventory and translator-capability tokens.

    Status policy (RESOLVED OQ-1, cite in README): required = OM-001, OM-006, OM-097, OM-099 + every hardware-conditional opinion (OM-024..027, OM-068..094 hardware ones — their hardware_condition already gates them); nice-to-have = themes OM-114..OM-134; all others keep their natural inventory role (most package/config opinions required, optional extras nice-to-have) chosen so the speech resolves WITHOUT hard conflicts.

    Author the 32 point YAML files into examples/omarchy/points/ (one per point in research/omarchy-points.md), each with schema:1, id, name, intent, curator, and a members list referencing its OM-NNN opinions with per-member status. Coverage rule: every OM-001..OM-134 appears in exactly one point (verify against the Summary Statistics table).

    Copy examples/LICENSE (CC0) to examples/omarchy/LICENSE. Author examples/omarchy/README.md: explains the composition, cites the pinned Omarchy commit 9cf1852, the status policy, and that capability tokens map opinions to translator capabilities (ARCH-03).

    Each authored file MUST pass resolver/parse validation — validate as you go (the Go harness in Task 2 is the gate; you may also spot-check with a small Go snippet or the parse package).
  </action>
  <verify>
    <automated>test $(ls examples/omarchy/opinions/OM-*.yaml | wc -l) -eq 134 && test $(ls examples/omarchy/points/*.yaml | wc -l) -eq 32 && python -c "import yaml,glob; [yaml.safe_load(open(f)) for f in glob.glob('examples/omarchy/opinions/*.yaml')+glob.glob('examples/omarchy/points/*.yaml')]"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 134 opinion files: `ls examples/omarchy/opinions/OM-*.yaml | wc -l` == 134.
    - Exactly 32 point files: `ls examples/omarchy/points/*.yaml | wc -l` == 32.
    - All YAML parses: the python glob safe_load exits 0.
    - Coverage: a check that the union of all point members' OM-IDs equals OM-001..OM-134 with no dupes — `python -c "import yaml,glob,re; ids=[]; [ids.extend(m['id'] for m in yaml.safe_load(open(f)).get('members',[])) for f in glob.glob('examples/omarchy/points/*.yaml')]; assert len(ids)==134 and len(set(ids))==134, (len(ids),len(set(ids)))"` exits 0.
    - Themes are nice-to-have: `grep -l 'OM-114\|OM-132' examples/omarchy/opinions/*.yaml` shows status nice-to-have (spot check OM-132 default theme has status: nice-to-have).
    - examples/omarchy/LICENSE is CC0: `grep -q 'CC0' examples/omarchy/LICENSE` exits 0.
  </acceptance_criteria>
  <done>134 opinions + 32 points authored as schema-valid YAML; coverage rule satisfied (each OM exactly once); status policy applied; CC0 license + README present.</done>
</task>

<task type="auto">
  <name>Task 2: speech.yaml + Go harness asserting clean resolution</name>
  <files>examples/omarchy/speech.yaml, examples/omarchy_test.go</files>
  <read_first>
    - examples/examples_test.go (loadExample/findRoot/Resolve usage; the omarchy-mini test asserts non-empty InstallOrder/Applied — mirror that, but for the FULL speech)
    - resolver/resolve/explanation.go (ResolvedSpeech fields: InstallOrder, Applied, Skipped, Dropped, Explanations; "Hard conflict" appears in Explanation.Text or the returned error on a hard conflict)
    - examples/omarchy/points (authored in Task 1 — speech references these point ids)
  </read_first>
  <action>
    Author examples/omarchy/speech.yaml: schema:1, id, name, foundation: arch, points: list referencing all 32 point ids. Optionally include a hardware profile block so hardware-conditional opinions resolve to Skipped on a baseline (no special hardware) target — the north-star vanilla-arch target is a generic machine, so most OM-024..027/070..094 hardware opinions will be Skipped (this is correct and expected; the test asserts no HARD conflict, not that everything applies).

    Author examples/omarchy_test.go (package examples_test): add a TestExampleOmarchy that loads examples/omarchy via a loader that reads ALL opinion YAML files in opinions/ AND resolves the points→opinions through the speech. Because the existing loadExample expects a single opinions.yaml, write an omarchy-specific loader (or extend) that globs opinions/*.yaml and points/*.yaml. Assert: Resolve returns no error (or returns a ResolvedSpeech with NO Explanation whose Text contains "Hard conflict"); InstallOrder non-empty; Applied non-empty; every Applied id has the OM- prefix; hardware-gated opinions on a baseline profile land in Skipped (not error).

    Keep the harness pattern consistent with examples_test.go (findRoot, t.Helper, table-free single test is fine). This test is the ARCH-02 clean-resolution gate.
  </action>
  <verify>
    <automated>go test ./examples/ -run TestExampleOmarchy -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./examples/ -run TestExampleOmarchy -count=1` exits 0.
    - speech targets arch: `grep -q 'foundation: *.*arch' examples/omarchy/speech.yaml` exits 0.
    - speech references 32 points: `grep -c 'id:' examples/omarchy/speech.yaml` ≥ 32 (under points:).
    - The test asserts no hard conflict: `grep -q 'Hard conflict' examples/omarchy_test.go` exits 0 (the assertion checks for its ABSENCE).
    - Full regression stays green: `go test ./... -count=1` exits 0.
  </acceptance_criteria>
  <done>speech.yaml composes all 32 points targeting vanilla Arch; TestExampleOmarchy proves the speech resolves with no hard conflicts, non-empty install order, and OM-prefixed applied opinions; full Go suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored opinion YAML → resolver/translator | Example content is CC0 and locally authored; in Phase 5 such content is registry-sourced (attacker-controlled). Opinion script_payloads are data, never executed here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | OM-001 custom repo (sig_level OptionalTrustAll) | mitigate | OM-001 carries the repo with its declared sig_level and the signing-key fingerprint embedded (RESOLVED OQ-2); resolver emits TrustWarning (T-01-10); translator surfaces it downstream |
| T-02-07 | Spoofing | script_payload opinions misrepresenting required capabilities | mitigate | Each script-bearing opinion declares translator_capabilities; the capability gate (Plan 01) validates them at composition time |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — YAML/Go authoring only; the one-off Python author-helper uses stdlib + PyYAML (Arch-official) |
</threat_model>

<verification>
- `go test ./examples/ -run TestExampleOmarchy -count=1 -v` GREEN.
- `go test ./... -count=1` GREEN (no regression).
- Coverage check: 134 opinions, 32 points, each OM exactly once.
</verification>

<success_criteria>
- ARCH-02 (content half): the full Omarchy speech resolves cleanly with no hard conflicts.
- All 134 opinions + 32 points are schema-valid and pass resolver/parse.
- Status policy from the RESOLVED open question applied; pinned commit cited; CC0 licensed.
- Opinion directory is usable as the translator's opinion-body source for Plan 05.
</success_criteria>

<output>
Create `.planning/phases/02-arch-translator/02-04-SUMMARY.md` when done.
</output>
