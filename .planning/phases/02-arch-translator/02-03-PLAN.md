---
phase: 02-arch-translator
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - translators/arch/profiles/vanilla-arch.yaml
  - translators/arch/profiles/cachyos.yaml
  - translators/arch/profiles/garuda.yaml
  - translators/arch/profiles/README.md
autonomous: true
requirements: [ARCH-04]
user_setup: []

must_haves:
  truths:
    - "Three declarative variant profile YAML files exist: vanilla-arch, cachyos, garuda"
    - "vanilla-arch declares no custom repos (the baseline north-star target)"
    - "cachyos declares its repos, keyring-before-repos ordering, kernel package, and pre-seeded opinions from the verified delta study"
    - "garuda declares chaotic-aur repo, keyring, and explicitly marks its hard conflicts with Omarchy (dracut vs mkinitcpio, GRUB vs limine, mandatory snapper, SDDM theme)"
    - "Every [UNVERIFIED] datum from the delta study is tagged [UNVERIFIED] in the YAML"
  artifacts:
    - path: "translators/arch/profiles/vanilla-arch.yaml"
      provides: "Baseline variant profile (no custom repos)"
      contains: "variant: vanilla-arch"
    - path: "translators/arch/profiles/cachyos.yaml"
      provides: "CachyOS variant profile (repos + keyring + kernel + pre-seeded)"
      contains: "cachyos-keyring"
    - path: "translators/arch/profiles/garuda.yaml"
      provides: "Garuda variant profile with marked Omarchy conflicts"
      contains: "conflicts_with_omarchy"
  key_links:
    - from: "translators/arch/profiles/cachyos.yaml"
      to: "translators/arch/variant.py (Plan 02)"
      via: "variant loader consumes these keys to inject repos/keyring into pacman.conf"
      pattern: "keyring_install_before_repos"
---

<objective>
Author the three declarative variant profile YAML files (ARCH-04) that parameterize the single Arch generator: `vanilla-arch.yaml` (baseline, north-star target), `cachyos.yaml`, and `garuda.yaml`. These are pure data — no code — derived strictly from the verified Phase 0 delta study. The generator (Plan 02) consumes them without any per-variant code fork.

Purpose: ARCH-04 requires "no per-variant forks" — variant differences must live entirely in declarative YAML. This plan produces that data so Plan 02's `variant.py` has a stable schema to load and test against. Garuda's documented hard conflicts with Omarchy are captured as data (Omarchy-on-Garuda is a non-gating stretch per CONTEXT, but the profile and its conflict markers ARE Phase 2 work).

Output: `translators/arch/profiles/{vanilla-arch,cachyos,garuda}.yaml` + a README documenting the profile schema.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-arch-translator/02-CONTEXT.md
@.planning/phases/02-arch-translator/02-RESEARCH.md

# THE authoritative variant data — repos, keyring, kernel, pre-seeded opinions, conflicts,
# and every [UNVERIFIED] tag. Copy facts FROM here; do not re-derive from blog posts.
@research/arch-variants-delta.md
</context>

<artifacts_this_phase_produces>
Variant profile YAML schema (consumed by `translators/arch/variant.py` in Plan 02). Keys:
variant, description, repos (ordered list of {name,url,sig_level,above_core}),
keyring_install_before_repos (list), kernel ({package,headers}),
defaults ({initramfs,bootloader,filesystem}), pre_seeded_opinions (list),
conflicts_with_omarchy (list — garuda only). Full schema documented in profiles/README.md.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: vanilla-arch + cachyos profiles + schema README</name>
  <files>translators/arch/profiles/vanilla-arch.yaml, translators/arch/profiles/cachyos.yaml, translators/arch/profiles/README.md</files>
  <read_first>
    - research/arch-variants-delta.md §CachyOS (Custom Repos, Keyring, Kernel, CachyOS-Settings pre-seeded opinions, FS/bootloader rows — note which are [UNVERIFIED])
    - 02-RESEARCH.md §Variant Profiles (vanilla-arch.yaml and cachyos.yaml sketches with VERIFIED facts)
  </read_first>
  <action>
    Author vanilla-arch.yaml exactly per the baseline sketch (research §Variant Profiles): variant=vanilla-arch, empty repos, empty keyring_install_before_repos, kernel.package=linux / headers=linux-headers, defaults.initramfs=mkinitcpio, bootloader=null, filesystem=null, pre_seeded_opinions=[]. This is the north-star target profile.

    Author cachyos.yaml from the VERIFIED delta-study facts: repos including [cachyos] (note the v3/v4 optimized repos as optional entries), mirror url https://cdn77.cachyos.org/repo/$arch/$repo, above_core=true for cachyos-* repos; keyring_install_before_repos=[cachyos-keyring]; kernel.package=linux-cachyos / headers=linux-cachyos-headers; defaults.initramfs=mkinitcpio; bootloader/filesystem=null with an [UNVERIFIED] note (CachyOS installer presents the choice); pre_seeded_opinions listing the 12 sysctl params in 70-cachyos-settings.conf plus the kernel pre-seed conflict note (linux-cachyos pre-installed → required-vs-pre-seeded against an Omarchy linux opinion). Tag every datum the delta study marks [UNVERIFIED] with an [UNVERIFIED] note in the YAML.

    Author profiles/README.md documenting the variant-profile schema (the keys listed in artifacts_this_phase_produces) and stating the ARCH-04 invariant: one generator, no per-variant code forks; differences are data only.
  </action>
  <verify>
    <automated>python -c "import yaml; [yaml.safe_load(open(f)) for f in ['translators/arch/profiles/vanilla-arch.yaml','translators/arch/profiles/cachyos.yaml']]" && grep -q 'cachyos-keyring' translators/arch/profiles/cachyos.yaml && grep -q 'variant: vanilla-arch' translators/arch/profiles/vanilla-arch.yaml</automated>
  </verify>
  <acceptance_criteria>
    - Both YAML files parse with yaml.safe_load (exit 0).
    - `grep -c 'variant:' translators/arch/profiles/vanilla-arch.yaml` ≥ 1 and value is vanilla-arch.
    - `grep -q 'cachyos-keyring' translators/arch/profiles/cachyos.yaml` exits 0.
    - `grep -q 'cdn77.cachyos.org' translators/arch/profiles/cachyos.yaml` exits 0.
    - `grep -q 'UNVERIFIED' translators/arch/profiles/cachyos.yaml` exits 0 (delta study has [UNVERIFIED] items that must be preserved).
    - profiles/README.md documents all schema keys: `grep -c 'keyring_install_before_repos\|pre_seeded_opinions\|conflicts_with_omarchy' translators/arch/profiles/README.md` ≥ 3.
  </acceptance_criteria>
  <done>vanilla-arch.yaml is the empty-repos baseline; cachyos.yaml carries verified repos/keyring/kernel/pre-seeded data with [UNVERIFIED] tags preserved; README documents the schema and the no-fork invariant.</done>
</task>

<task type="auto">
  <name>Task 2: garuda profile with marked Omarchy conflicts</name>
  <files>translators/arch/profiles/garuda.yaml</files>
  <read_first>
    - research/arch-variants-delta.md §Garuda (Custom Repos, Keyring, Kernel, initramfs=dracut, btrfs/GRUB/snapper, SDDM theme, and all [UNVERIFIED] tags)
    - 02-RESEARCH.md §Variant Profiles (garuda.yaml verified facts + the four hard conflicts) and §Note for planner (Garuda profile is Phase 2; full ISO validation is not)
  </read_first>
  <action>
    Author garuda.yaml: repos=[chaotic-aur] (confirmed) plus [garuda] tagged [UNVERIFIED] for its URL; keyring_install_before_repos=[chaotic-keyring] with garuda-keyring tagged [UNVERIFIED]; kernel.package=linux (or linux-zen note) / headers=linux-headers (no custom kernel — Garuda uses stock); defaults.initramfs=dracut, bootloader=grub, filesystem=btrfs.

    Add a conflicts_with_omarchy list with one entry per hard conflict, each naming the Garuda mechanism, the Omarchy mechanism it collides with, and the affected Omarchy opinion(s) where known:
      1. initramfs: garuda dracut (garuda-dracut-support conflicts mkinitcpio) vs Omarchy mkinitcpio (OM-002/OM-086/OM-091/OM-099 use mkinitcpio).
      2. bootloader: garuda GRUB mandatory vs Omarchy limine (OM-099 configure-bootloader-limine).
      3. snapper: garuda mandatory root snapper vs Omarchy own snapper config (OM-099 create-snapper-config).
      4. sddm theme: garuda Dr460nized vs Omarchy SDDM theme (OM-097).
    Mark the whole garuda variant with a top-level note: "Omarchy-on-Garuda is a non-gating stretch (post-v1.0); this profile and its conflict markers are Phase 2 deliverables, full ISO validation is not."
  </action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('translators/arch/profiles/garuda.yaml')); assert d['variant']=='garuda'; assert isinstance(d['conflicts_with_omarchy'], list) and len(d['conflicts_with_omarchy'])>=4" && grep -q 'chaotic-keyring' translators/arch/profiles/garuda.yaml && grep -q 'dracut' translators/arch/profiles/garuda.yaml</automated>
  </verify>
  <acceptance_criteria>
    - garuda.yaml parses; variant==garuda; conflicts_with_omarchy has ≥ 4 entries (exit 0 on the python assert).
    - `grep -q 'chaotic-keyring' translators/arch/profiles/garuda.yaml` exits 0.
    - `grep -Ei 'dracut|grub|snapper|sddm' translators/arch/profiles/garuda.yaml | grep -c .` ≥ 4 (all four conflict mechanisms present).
    - `grep -q 'UNVERIFIED' translators/arch/profiles/garuda.yaml` exits 0 ([garuda] repo URL + keyring are [UNVERIFIED] in the delta study).
    - All three profile YAMLs parse together: `python -c "import yaml,glob; [yaml.safe_load(open(f)) for f in glob.glob('translators/arch/profiles/*.yaml')]"` exits 0.
  </acceptance_criteria>
  <done>garuda.yaml declares chaotic-aur + keyring + dracut/grub/btrfs defaults and marks all four hard conflicts with Omarchy as data, with [UNVERIFIED] items preserved and the non-gating-stretch note present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| variant YAML → generated pacman.conf | Repo URLs and sig_levels from variant data become live pacman repos; sig_level=Never repos are a trust downgrade. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing | Variant repos with weak/absent signature verification | mitigate | sig_level recorded per repo; the generator (Plan 02) surfaces non-Required sig_levels as pacman.conf warning comments; keyring_install_before_repos enforces keyring-first ordering (research Pitfall 4) |
| T-02-05 | Information disclosure | [UNVERIFIED] data treated as fact downstream | accept | All unverified data tagged [UNVERIFIED] in-line per delta study; consumers (and humans) see the tag; full variant ISO validation is non-gating |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan — pure YAML/markdown authoring |
</threat_model>

<verification>
- `python -c "import yaml,glob; [yaml.safe_load(open(f)) for f in glob.glob('translators/arch/profiles/*.yaml')]"` parses all three.
- All [UNVERIFIED] markers from the delta study are preserved (grep across the three files).
- `go test ./... -count=1` unaffected (no Go changes).
</verification>

<success_criteria>
- ARCH-04: three declarative variant profiles exist as data only (no code), with the schema documented and the no-fork invariant stated.
- CachyOS/Garuda verified facts copied faithfully from the delta study; [UNVERIFIED] items tagged.
- Garuda's four hard Omarchy conflicts captured as structured data.
</success_criteria>

<output>
Create `.planning/phases/02-arch-translator/02-03-SUMMARY.md` when done.
</output>
