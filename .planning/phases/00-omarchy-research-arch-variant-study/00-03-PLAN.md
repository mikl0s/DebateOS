---
phase: 00-omarchy-research-arch-variant-study
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - research/arch-variants-delta.md
autonomous: true
requirements: [RSCH-02]
must_haves:
  truths:
    - "Both CachyOS and Garuda delta sections present, derived from freshly cloned public repos (not the RESEARCH summary)"
    - "Each variant catalogs repos, keyring, kernel, default fs/bootloader, and pre-seeded opinions"
    - "A declarative variant-profile YAML sketch is present for each variant"
    - "Every variant claim cites a pinned source (repo URL + commit/date)"
  artifacts:
    - path: "research/arch-variants-delta.md"
      provides: "CachyOS + Garuda delta catalog and variant-profile candidate shape"
      contains: "variant-profile"
      min_lines: 150
  key_links:
    - from: "research/arch-variants-delta.md"
      to: "cloned CachyOS/Garuda repos"
      via: "pinned source citations (URL + commit/date)"
      pattern: "github.com"
---

<objective>
Produce research/arch-variants-delta.md: the CachyOS + Garuda substitution study. Catalog each variant's delta from vanilla Arch (repos, keyrings, kernels, default fs/bootloader, pre-seeded configs) and propose a declarative variant-profile shape (repo list + keyring + kernel + defaults — not a fork per variant). Implements RSCH-02 (D20).

Purpose: Researcher confidence on variants is LOW (web-only). The executor MUST clone the actual variant repos and verify claims before writing. Feeds Phase 2 translator design and the Plan 04 edge-case corpus.
Output: research/arch-variants-delta.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-omarchy-research-arch-variant-study/00-RESEARCH.md
@.planning/phases/00-omarchy-research-arch-variant-study/00-VALIDATION.md
@.planning/phases/00-omarchy-research-arch-variant-study/00-CONTEXT.md
@docs/08-omarchy-research.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone and verify CachyOS + Garuda repos; catalog deltas from vanilla Arch</name>
  <files>research/arch-variants-delta.md</files>
  <read_first>
    - 00-RESEARCH.md "Arch-Variant Delta Study" (tables + candidate profiles), Pitfall 4 (use GitHub mirrors), "Assumptions Log" A1-A7, "Environment Availability" (clone cmds), "Security Domain"
    - 00-VALIDATION.md "Manual-Only Verifications" (verify against live repos, not the summary)
  </read_first>
  <action>
    The RESEARCH variant data is LOW confidence (web-only). Clone the repos fresh (depth=1) and verify before writing — do NOT just transcribe 00-RESEARCH. Use GitHub mirrors (Pitfall 4 — GitLab needs auth):
    CachyOS: github.com/CachyOS/CachyOS-PKGBUILDS, github.com/CachyOS/linux-cachyos, github.com/CachyOS/CachyOS-Settings (also fetch github.com/CachyOS/docker pacman-v3.conf for repo ordering).
    Garuda: github.com/garuda-linux/pkgbuilds, github.com/garuda-linux/garuda-tools.
    Record git rev-parse HEAD (or fetch date) for EVERY source. Do NOT execute any cloned PKGBUILD/script — read-only.

    Create research/arch-variants-delta.md with a header noting the Omarchy pin 9cf1852525a5f7de26d3162db9d61e2f5c1d5523 AND each variant repo's commit/date. Write "## CachyOS" and "## Garuda" sections. For EACH variant catalog: custom repos (names + URLs + priority vs Arch core/extra/multilib), keyring package, kernel variant(s), default filesystem + bootloader, pre-seeded opinions (configs/tooling overlapping Omarchy opinions). Verify/correct the key claims against cloned source: CachyOS repo layering (cachyos / cachyos-v3 / cachyos-v4 above Arch) + linux-cachyos kernel family + cachyos-keyring; Garuda [garuda]+[chaotic-aur] + chaotic-keyring + btrfs/snapper/grub-btrfs default + Dr460nized theming + garuda-update + btrfs subvolume layout. Tag any unverifiable claim [UNVERIFIED]. Model foundation pre-seeded opinions as a pre_seeded_opinions list (CONTEXT); defer conflict semantics to Phase 1 (record the open question for Plan 04). Capture per-repo trust level where unsigned (Security Domain).

    Write the gate to /tmp/check-variants-src.sh: assert both "## CachyOS" and "## Garuda" present, grep -c keyring >= 2, and >= 4 github.com source references each with an adjacent commit-hash or date marker. The verify runs it.
  </action>
  <verify>
    <automated>bash /tmp/check-variants-src.sh</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/check-variants-src.sh exits 0
    - Both "## CachyOS" and "## Garuda" sections present; each names repos, kernel, default fs/bootloader, pre_seeded_opinions
    - grep -c keyring >= 2
    - Every variant claim cites a pinned source (URL + commit/date); unverifiable claims tagged [UNVERIFIED]
    - Header pins Omarchy commit and the variant source commits/dates
  </acceptance_criteria>
  <done>Both variants cataloged from freshly cloned, pinned sources; claims verified or flagged; deltas cover repos/keyring/kernel/fs/bootloader/pre-seeded opinions.</done>
</task>

<task type="auto">
  <name>Task 2: Propose the declarative variant-profile YAML shape for both variants</name>
  <files>research/arch-variants-delta.md</files>
  <read_first>
    - research/arch-variants-delta.md (Task 1 delta catalog)
    - 00-RESEARCH.md variant-profile candidate fields
    - 00-CONTEXT.md "Variant-Profile Proposal Shape" (candidate sketch only; final schema Phase 1)
  </read_first>
  <action>
    Add a "## Variant-Profile Shape" section with a declarative YAML sketch for EACH variant (CachyOS and Garuda), CANDIDATE only — final schema is Phase 1 (D17). Each sketch includes: variant name, repos (name + url/mirrorlist + keyring + priority vs Arch), kernel (package + headers), defaults (filesystem, bootloader, cpu arch level where relevant), and pre_seeded_opinions referencing overlapping capabilities. The shape must be repo-list + keyring + kernel + defaults — explicitly NOT a per-variant fork (D20 anti-bloat). Label each block with "variant-profile" so the gate finds it.

    Write the gate to /tmp/check-variants-profile.sh: >= 1 "variant-profile" marker, "repos:" present under it, both a cachyos and a garuda profile present. The verify runs it.
  </action>
  <verify>
    <automated>bash /tmp/check-variants-profile.sh</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/check-variants-profile.sh exits 0
    - >= 1 "variant-profile" labeled YAML block containing "repos:"
    - A CachyOS profile (repos + cachyos-keyring + linux-cachyos kernel + cpu arch level) and a Garuda profile (repos + chaotic-keyring + btrfs/grub defaults + subvolume layout) both present
    - Each profile carries a pre_seeded_opinions list; no per-variant fork proposed
  </acceptance_criteria>
  <done>Declarative variant-profile sketches for both variants written as candidates; repo-list+keyring+kernel+defaults shape, no forks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party variant repos → researcher's machine | Cloning CachyOS/Garuda public mirrors; PKGBUILDs and configs read, never executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-VAR | Tampering | cloned variant PKGBUILDs/configs | mitigate | Read-only analysis; never execute cloned PKGBUILD/script; pin each source by commit/date for reproducibility |
| T-00-VSIG | Tampering | unsigned/weak-trust variant repos | accept | Document keyring + trust level per repo (e.g. chaotic-keyring, cachyos-keyring); no repo added to this machine — documentation only |
</threat_model>

<verification>
After each task run the RSCH-02 checks from 00-VALIDATION.md:
- /tmp/check-variants-src.sh (both variants, keyrings, pinned sources)
- /tmp/check-variants-profile.sh (variant-profile sketch present for both)
</verification>

<success_criteria>
- research/arch-variants-delta.md: both variants cataloged from cloned pinned sources, all claims cited or [UNVERIFIED]
- Declarative variant-profile sketch for both variants; no per-variant fork
- Header pins Omarchy + variant source commits/dates
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's portion)
- research/arch-variants-delta.md — CachyOS + Garuda delta catalog
- variant-profile YAML sketch (candidate) for CachyOS and Garuda — repo list + keyring + kernel + defaults + pre_seeded_opinions; consumed by Phase 2 Arch translator design and the Plan 04 edge-case corpus
</artifacts_produced>

<output>
Create `.planning/phases/00-omarchy-research-arch-variant-study/00-03-SUMMARY.md` when done
</output>
