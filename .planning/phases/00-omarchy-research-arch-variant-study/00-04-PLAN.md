---
phase: 00-omarchy-research-arch-variant-study
plan: 04
type: execute
wave: 3
depends_on: ["00-01", "00-03"]
files_modified:
  - research/resolver-edge-cases.md
  - research/open-questions.md
autonomous: true
requirements: [RSCH-03, RSCH-01]
must_haves:
  truths:
    - "resolver-edge-cases.md has >= 15 EC-NNN scenarios, each in Given/When/Then form with expected explanation text and a provenance tag"
    - "A coverage matrix maps all 8 docs/04 resolution rules to at least one EC-NNN"
    - "All 6 RESEARCH collision classes (incl. CachyOS kernel + Garuda theming + repo-priority) are represented as EC-NNN scenarios"
    - "open-questions.md lists >= 5 questions including migrations primitive, execution-phase field, and runtime-tool-install category"
  artifacts:
    - path: "research/resolver-edge-cases.md"
      provides: "EC-NNN Given/When/Then corpus seeding the Phase 1 TDD harness"
      contains: "EC-"
      min_lines: 200
    - path: "research/open-questions.md"
      provides: "Surprises, ambiguities, deferred schema questions"
      contains: "?"
      min_lines: 40
  key_links:
    - from: "research/resolver-edge-cases.md"
      to: "docs/04-conflict-resolution.md"
      via: "coverage matrix over the 8 resolution rules"
      pattern: "EC-NNN"
    - from: "research/resolver-edge-cases.md"
      to: "research/arch-variants-delta.md"
      via: "variant collision scenarios"
      pattern: "EC-NNN"
---

<objective>
Produce research/resolver-edge-cases.md (the EC-NNN Given/When/Then conflict corpus that directly seeds the Phase 1 TDD harness per D19) and research/open-questions.md (surprises, ambiguities, deferred schema questions). Implements RSCH-03 and the open-questions portion of RSCH-01. This is the final wave — it consumes the inventory (Plan 01) and the variant deltas (Plan 03).

Purpose: Per D19, the edge-case corpus IS the seed of the Phase 1 resolver test harness; each EC traces 1:1 to a future test. The EC-NNN Given/When/Then + provenance format is a hard requirement.
Output: research/resolver-edge-cases.md, research/open-questions.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-omarchy-research-arch-variant-study/00-RESEARCH.md
@.planning/phases/00-omarchy-research-arch-variant-study/00-VALIDATION.md
@.planning/phases/00-omarchy-research-arch-variant-study/00-CONTEXT.md
@docs/04-conflict-resolution.md
@research/omarchy-opinion-inventory.md
@research/arch-variants-delta.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the EC-NNN resolver edge-case corpus with full docs/04 + collision-class coverage</name>
  <files>research/resolver-edge-cases.md</files>
  <read_first>
    - docs/04-conflict-resolution.md — the 8 resolution rules (required>nice-to-have; required-vs-required hard conflict; required-vs-required-with-patch; nice-vs-nice default; patch overrides hierarchy; ordering/toposort; cycle detection; hardware-conditional)
    - 00-RESEARCH.md "Resolver Edge-Case Corpus: Confirmed Collision Classes" (Classes 1-6, EC-001..EC-051), "Class 4 docs/04 Rule Coverage" table
    - 00-CONTEXT.md "Resolver Edge-Case Corpus" (Given/When/Then, EC-NNN, expected explanation text, coverage matrix, provenance)
    - research/omarchy-opinion-inventory.md and research/arch-variants-delta.md — to ground scenarios in real OM-NNN opinions and variant deltas
  </read_first>
  <action>
    Create research/resolver-edge-cases.md with the pinned Omarchy commit in the header. Write >= 15 EC-NNN scenarios, each with: an "### EC-NNN" heading, a Given/When/Then body, expected explanation text the resolver should emit, and a provenance tag (evidence-backed vs synthesized — per CONTEXT). Ground scenarios in real OM-NNN opinions (from the inventory) and real variant deltas (from arch-variants-delta.md) wherever possible.

    Coverage is mandatory. Include a "## Coverage Matrix" mapping each of the 8 docs/04 rules to >= 1 EC-NNN: required-beats-nice-to-have, required-vs-required hard conflict, required-vs-required-with-patch, nice-vs-nice default, patch overrides hierarchy, ordering/toposort, cycle detection, hardware-conditional (both skip and apply). Also cover all 6 RESEARCH collision classes — at minimum the CONTEXT-mandated variant collisions: kernel-default vs kernel-opinion (CachyOS), theming collision (Garuda SDDM/plymouth), and repo-priority conflict (CachyOS/Chaotic-AUR). Synthesized scenarios are allowed where the hierarchy needs coverage (e.g. required-vs-required with patch) but MUST be tagged provenance: synthesized.

    Write the gate to /tmp/check-ec.sh: assert "### EC-" count >= 15; assert each docs/04 rule keyword present (required beats / hard conflict / patch / nice.*nice / toposort|ordering / cycle / hardware-conditional); assert CachyOS and Garuda both referenced; assert every EC block has Given/When/Then and a provenance tag. The verify runs it.
  </action>
  <verify>
    <automated>bash /tmp/check-ec.sh</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/check-ec.sh exits 0
    - grep -c "### EC-" research/resolver-edge-cases.md >= 15
    - Coverage Matrix maps all 8 docs/04 rules each to >= 1 EC-NNN
    - CachyOS kernel collision, Garuda theming collision, and a repo-priority conflict each present as EC-NNN
    - Every EC has Given/When/Then, expected explanation text, and a provenance tag (evidence-backed | synthesized)
    - Header pins commit 9cf1852525a5f7de26d3162db9d61e2f5c1d5523
  </acceptance_criteria>
  <done>>= 15 EC-NNN Given/When/Then scenarios written; all 8 docs/04 rules and all mandated variant collisions covered; every EC has explanation text + provenance; ready to seed Phase 1 TDD.</done>
</task>

<task type="auto">
  <name>Task 2: Write open-questions.md capturing surprises, ambiguities, and deferred schema questions</name>
  <files>research/open-questions.md</files>
  <read_first>
    - 00-RESEARCH.md "Open Questions" (the 6 enumerated questions: tag pinning, migrations as schema concept, omarchy custom repo as opinion, npm/runtime-tool category, first-run vs install-time execution phase, CachyOS CPU arch detection)
    - research/omarchy-opinion-inventory.md — the "Coverage notes / unmapped scripts" section from Plan 01
    - 00-CONTEXT.md "Deferred Ideas" (variant-profile conflict semantics deferred to Phase 1 — record here)
  </read_first>
  <action>
    Create research/open-questions.md with the pinned commit in the header. List >= 5 open questions. MUST include at minimum: (1) migrations-as-schema-concept primitive (313 timestamped migrations — does the schema need a migration concept or is opinion version pinning enough); (2) execution-phase field (first-run vs install-time, headless CI can't run first-run opinions); (3) runtime-tool-install category distinct from OS packages (npm-global AI tools); plus the remaining RESEARCH open questions (tag pinning given no git tags; omarchy custom repo as opinion vs prerequisite; CachyOS v3/v4 static-vs-dynamic variant selection). Also record the CONTEXT deferred item: variant-profile conflict semantics (foundation pre-seeded opinion vs user opinion) deferred to Phase 1. Fold in any unmapped scripts flagged in the inventory's Coverage notes section.

    Each question must be phrased as a question, note the recommended lean where RESEARCH gave one, and tag where it is resolved (Phase 1 schema design vs ongoing). Do NOT resolve deferred items here.

    Write the gate to /tmp/check-oq.sh: assert >= 5 "?" question entries and that the keywords migration, execution-phase (or first-run), and runtime-tool (or npm) each appear. The verify runs it.
  </action>
  <verify>
    <automated>bash /tmp/check-oq.sh</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/check-oq.sh exits 0
    - >= 5 distinct open questions, each phrased as a question
    - Migrations primitive, execution-phase field, and runtime-tool-install category all present
    - Variant-profile conflict semantics recorded as deferred to Phase 1 (CONTEXT deferred idea)
    - Any unmapped scripts from the inventory Coverage notes are referenced
    - Header pins commit 9cf1852525a5f7de26d3162db9d61e2f5c1d5523
  </acceptance_criteria>
  <done>open-questions.md lists >= 5 questions incl. the three mandated ones; deferred items recorded not resolved; unmapped scripts folded in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research artifacts → Phase 1 TDD harness | EC-NNN corpus and open questions flow into Phase 1 design/tests; no execution in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-PROV | Repudiation | unmarked synthesized edge cases | mitigate | Every EC carries a provenance tag (evidence-backed vs synthesized) so Phase 1 can trust evidence-backed scenarios and scrutinize synthesized ones |
| T-00-SIGEC | Tampering | repo-priority / unsigned-repo edge cases | accept | Repo-priority and SigLevel collisions are documented as resolver scenarios only; no repos are installed |
</threat_model>

<verification>
After each task run the RSCH-03 + open-questions checks from 00-VALIDATION.md:
- /tmp/check-ec.sh (>= 15 EC, all 8 docs/04 rules, variant collisions, Given/When/Then + provenance)
- /tmp/check-oq.sh (>= 5 questions incl. migrations, execution-phase, runtime-tool)
</verification>

<success_criteria>
- research/resolver-edge-cases.md: >= 15 EC-NNN, full docs/04 coverage matrix, all mandated variant collisions, every EC has explanation + provenance
- research/open-questions.md: >= 5 questions incl. the three mandated; deferred items recorded
- Both headers pin commit 9cf1852525a5f7de26d3162db9d61e2f5c1d5523
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's portion)
- research/resolver-edge-cases.md — EC-NNN Given/When/Then corpus; introduces the EC-NNN ID scheme (### EC-NNN, Given/When/Then + expected explanation + provenance tag), consumed 1:1 by the Phase 1 resolver TDD harness (D19)
- research/open-questions.md — surprises + deferred schema questions
- EC-NNN ID scheme definition + docs/04 coverage matrix
</artifacts_produced>

<output>
Create `.planning/phases/00-omarchy-research-arch-variant-study/00-04-SUMMARY.md` when done
</output>
