---
phase: 00-omarchy-research-arch-variant-study
plan: 02
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - research/omarchy-points.md
  - research/schema-requirements.md
autonomous: true
requirements: [RSCH-01]
must_haves:
  truths:
    - "Every OM-NNN from the inventory appears in exactly one point (or an explicit justified unassigned list)"
    - "Every schema requirement SR-NNN cites at least one OM-NNN or variant-evidence ID"
    - "The seven RESEARCH schema surprises each map to at least one SR-NNN"
  artifacts:
    - path: "research/omarchy-points.md"
      provides: "Candidate point groupings over the OM-NNN inventory"
      contains: "OM-"
      min_lines: 80
    - path: "research/schema-requirements.md"
      provides: "Minimum opinion/point/speech schema expressive surface, evidence-backed"
      contains: "SR-"
      min_lines: 120
  key_links:
    - from: "research/omarchy-points.md"
      to: "research/omarchy-opinion-inventory.md"
      via: "OM-NNN references"
      pattern: "OM-NNN"
    - from: "research/schema-requirements.md"
      to: "research/omarchy-opinion-inventory.md"
      via: "SR-NNN cites OM-NNN"
      pattern: "OM-NNN"
---

<objective>
Produce research/omarchy-points.md (group OM-NNN opinions into natural candidate points) and research/schema-requirements.md (minimum expressive surface an Opinion/Point/Speech schema must support, every requirement evidence-backed). Derives the Phase 1 schema floor (D17). Implements the grouping + schema-floor portion of RSCH-01.

Purpose: Phase 1 drafts the schema; this plan bounds it with real Omarchy evidence including the seven confirmed schema surprises.
Output: research/omarchy-points.md, research/schema-requirements.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-omarchy-research-arch-variant-study/00-RESEARCH.md
@.planning/phases/00-omarchy-research-arch-variant-study/00-VALIDATION.md
@docs/04-conflict-resolution.md
@docs/08-omarchy-research.md
@research/omarchy-opinion-inventory.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Group the OM-NNN inventory into candidate points</name>
  <files>research/omarchy-points.md</files>
  <read_first>
    - research/omarchy-opinion-inventory.md (full OM-NNN inventory from Plan 01)
    - docs/08-omarchy-research.md Method step 4 (point examples)
    - 00-CONTEXT.md Claude's Discretion (point count is evidence-driven)
  </read_first>
  <action>
    Read the complete OM-NNN inventory. Group opinions into natural candidate points a curator would compose as a unit (Hyprland desktop, developer toolchain, AI tooling, theming, Apple display support, hardware bundles, etc.). Grouping happens ONLY here, never in the inventory. Point count is evidence-driven, not a quota.

    Create research/omarchy-points.md with the pinned commit hash 9cf1852525a5f7de26d3162db9d61e2f5c1d5523 in the header. For each point record: name (heading "## Point: name"), one-line OS-agnostic intent, and the explicit member OM-NNN list.

    Coverage rule: every OM-NNN from the inventory appears in exactly one point; opinions that fit nowhere go under an explicit "## Unassigned" section with a reason each. No OM-NNN twice; no OM-NNN absent from the inventory. Write the gate to /tmp/check-points.sh (extract OM IDs from both files, sort -u, diff, fail on any diff or duplicate); the verify command runs it.
  </action>
  <verify>
    <automated>bash /tmp/check-points.sh</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/check-points.sh exits 0; OM-ID set in points equals OM-ID set in inventory (no ungrouped, no phantom)
    - No OM-NNN appears more than once in omarchy-points.md
    - Header contains commit 9cf1852525a5f7de26d3162db9d61e2f5c1d5523
    - At least 4 points, each with name, intent, member list
  </acceptance_criteria>
  <done>Every inventory opinion assigned to exactly one point (or justified unassigned); no phantoms, no double-assignment.</done>
</task>

<task type="auto">
  <name>Task 2: Derive the evidence-backed schema-requirements floor</name>
  <files>research/schema-requirements.md</files>
  <read_first>
    - research/omarchy-opinion-inventory.md (categories, condition, execution-phase fields)
    - research/omarchy-points.md (point/speech-level requirements)
    - 00-RESEARCH.md "Category Taxonomy" (7 schema surprises), "Security Domain"
    - docs/04-conflict-resolution.md (validated metadata floor)
  </read_first>
  <action>
    Create research/schema-requirements.md with the pinned commit hash in the header. Define the minimum schema expressive surface as stable SR-NNN requirements. Start from the docs/04 floor (required|nice-to-have status, dependencies, conflicts, hardware conditions, ordering constraints, known patches, translator-capability requirements) and expand with the SEVEN confirmed schema surprises from 00-RESEARCH: (1) file-asset payloads; (2) custom package-repository registration with URL + SigLevel + keyring dependency, capturing Optional TrustAll and SigLevel=Never trust levels; (3) npm-global / runtime-tool installs distinct from the OS package manager; (4) phase-level ordering being load-bearing; (5) compound hardware-condition predicates (the 18 omarchy-hw-* helpers, PCI+DMI+battery composites); (6) arbitrary script payloads with declared translator capabilities; (7) first-run vs install-time execution-phase field.

    Each SR-NNN MUST cite at least one OM-NNN (or a variant-evidence reference) inline as justification — no requirement without evidence. Use heading format "## SR-NNN". Keep requirements OS-agnostic (invariant 1): describe what the schema must EXPRESS, not translator mechanics. Do NOT draft the YAML schema (Phase 1, D17).

    Write the gate to /tmp/check-schema.sh: SR count >= 7; evidence-citation count (OM-/EC-/variant refs) >= SR count; each of the 7 surprise keywords present at least once. The verify command runs it.
  </action>
  <verify>
    <automated>bash /tmp/check-schema.sh</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/check-schema.sh exits 0
    - grep -c "## SR-" research/schema-requirements.md is >= 7
    - Every SR-NNN cites at least one OM-NNN or variant-evidence reference
    - All 7 schema surprises explicitly represented (file asset, custom-repo, npm/runtime-tool, phase-ordering, compound hardware, arbitrary script, execution-phase)
    - Header contains commit 9cf1852525a5f7de26d3162db9d61e2f5c1d5523
    - No SR intent text leaks Arch mechanics (no pacman/AUR/mkarchiso in the requirement statements)
  </acceptance_criteria>
  <done>Schema-requirements floor written; every SR evidence-backed; all 7 surprises covered; nothing Arch-specific leaks into requirement statements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented opinion metadata → downstream schema | Inventory-derived claims (incl. trust levels of repos) flow into schema requirements; no execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-SIG2 | Tampering | repo trust levels recorded in schema-requirements | mitigate | SR for custom-repo MUST carry per-repo trust level (Optional TrustAll, SigLevel=Never) so unsigned repos are explicit in the schema floor |
| T-00-LEAK | Information Disclosure | Arch mechanics leaking into OS-agnostic requirements | mitigate | Acceptance gate forbids pacman/AUR/mkarchiso in SR statements (invariant 1) |
</threat_model>

<verification>
After each task, run the RSCH-01 grouping + schema checks from 00-VALIDATION.md:
- /tmp/check-points.sh (OM coverage parity, no duplicates)
- /tmp/check-schema.sh (SR count, evidence citations, 7 surprises present)
</verification>

<success_criteria>
- research/omarchy-points.md: full OM coverage, >= 4 points, no phantoms/duplicates
- research/schema-requirements.md: >= 7 SR-NNN, every one evidence-backed, all 7 surprises covered
- Both headers pin commit 9cf1852525a5f7de26d3162db9d61e2f5c1d5523
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's portion)
- research/omarchy-points.md — candidate point groupings over OM-NNN
- research/schema-requirements.md — schema floor; introduces the SR-NNN ID scheme (## SR-NNN, each citing OM-/variant evidence), consumed by Phase 1 schema design
- SR-NNN ID scheme definition
</artifacts_produced>

<output>
Create `.planning/phases/00-omarchy-research-arch-variant-study/00-02-SUMMARY.md` when done
</output>
