---
phase: 05-registry-forum-debate-ui
plan: 06
type: execute
wave: 3
depends_on: ["05-04", "05-05"]
files_modified:
  - cli/embed/embed.go
  - cli/embed/embed_test.go
  - cli/embed/web/index.html
  - cli/compose/serve.go
  - cli/compose/compose.go
  - cli/compose/compose_test.go
  - cmd/debateos/main.go
  - scripts/build-ui-dual.sh
  - scripts/forum-offline-check.sh
  - scripts/check-coverage.sh
  - .github/workflows/registry-index.yml
  - forum/deploy/oracle-a1.md
  - docs/registry-forum-ui.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [UI-02, REG-01, FORM-05, BRND-01]
must_haves:
  truths:
    - "The SvelteKit build is embedded via go:embed and served at localhost root by `debateos compose --serve`"
    - "The WASM binary is served with Content-Type application/wasm from the embedded server"
    - "The same build output deploys to GitHub Pages (BASE_PATH=/debateos) AND embeds in the CLI (BASE_PATH=) — two builds, dual delivery"
    - "scripts/forum-offline-check.sh proves compose→resolve works with the Forum process DOWN (invariant 4)"
    - "All 9 Phase-5 requirement IDs are marked Complete in REQUIREMENTS.md with evidence"
  artifacts:
    - path: "cli/embed/embed.go"
      provides: "//go:embed of web build + WebFS"
      contains: "go:embed"
    - path: "cli/compose/serve.go"
      provides: "ServeUI over embed.FS (fs.Sub + http.FileServer)"
      exports: ["ServeUI"]
    - path: "scripts/forum-offline-check.sh"
      provides: "Invariant-4 offline gate"
    - path: ".github/workflows/registry-index.yml"
      provides: "Actions index rebuild-on-commit (live run deferred)"
    - path: "docs/registry-forum-ui.md"
      provides: "Deploy notes (Pages + Oracle A1) + brand-voice doc pass"
  key_links:
    - from: "cli/compose/serve.go"
      to: "cli/embed.WebFS"
      via: "fs.Sub + http.FileServer(http.FS(sub))"
      pattern: "fs.Sub"
    - from: "scripts/build-ui-dual.sh"
      to: "cli/embed/web"
      via: "BASE_PATH= build copied into embed target"
      pattern: "cli/embed/web"
---

<objective>
Close Phase 5 and the v1.0 milestone: wire the dual-delivery embed (UI-02 — same SvelteKit build to GitHub Pages with BASE_PATH=/debateos AND go:embed in the CLI with BASE_PATH= so `debateos compose --serve` serves it offline at localhost root), prove the phase's hardest guarantee with scripts/forum-offline-check.sh (compose→resolve works with the Forum process DOWN — invariant 4), author the Actions index-rebuild-on-commit workflow (REG-01; live run deferred-to-CI), write the Oracle A1 + Pages deploy notes and the brand-voice doc pass (BRND-01), extend the coverage gate to forum/ + registry/ (≥85%), and finalize all 9 requirement IDs in REQUIREMENTS.md.

Purpose: UI-02 dual delivery + invariant-4 gate + REG-01 rebuild automation + D15 deploy docs + milestone close.
Output: cli/embed + serve subcommand + dual-build script + offline gate + Actions workflow + deploy/brand docs + coverage gate + REQUIREMENTS status.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-registry-forum-debate-ui/05-CONTEXT.md
@.planning/phases/05-registry-forum-debate-ui/05-RESEARCH.md

# Contracts (read first)
@cli/compose/compose.go
@cmd/debateos/main.go
@scripts/check-coverage.sh
@.github/workflows/reusable-build-speech.yml
@.planning/phases/05-registry-forum-debate-ui/05-04-SUMMARY.md
@.planning/phases/05-registry-forum-debate-ui/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: go:embed dual-delivery serve + `debateos compose --serve` + dual-build script</name>
  <read_first>05-RESEARCH.md Pattern 3 (cli/embed/embed.go //go:embed all:web, ServeUI with fs.Sub + http.FileServer; net/http auto-detects application/wasm) + Pitfall 4 (two builds: BASE_PATH=/debateos for Pages, BASE_PATH= for cli/embed); cli/compose/compose.go (existing Run signature — add --serve flag without breaking the preview path); cmd/debateos/main.go (compose dispatch); docs/11-repo-layout.md (cli/embed dual-consumption contract)</read_first>
  <files>cli/embed/embed.go, cli/embed/embed_test.go, cli/embed/web/index.html, cli/compose/serve.go, cli/compose/compose.go, cli/compose/compose_test.go, cmd/debateos/main.go, scripts/build-ui-dual.sh</files>
  <behavior>
    - TestServeUI (cli/embed): an httptest request to "/" through NewUIHandler returns 200 with index.html content (embedded build at root).
    - TestWasmContentType: a request for /debateos.wasm returns Content-Type application/wasm (net/http auto-detect, or explicit override if needed).
    - TestComposeServeFlag: compose.Run with --serve constructs the UI handler via a seam without blocking on ListenAndServe; the existing no-flag preview path still works unchanged (compose_test.go stays green).
    - RED FIRST: `! go test ./cli/embed/... -run TestServeUI` fails before embed.go + the placeholder web tree exist.
  </behavior>
  <action>
    Commit a tracked placeholder cli/embed/web/index.html (minimal "DebateOS — built UI goes here" page) so the `//go:embed all:web` directive always compiles even before a full UI build (build-ui-dual.sh overwrites it). cli/embed/embed.go: package embeddedui; `//go:embed all:web` var WebFS embed.FS (the all: prefix includes dotfiles like .nojekyll). cli/compose/serve.go: NewUIHandler() http.Handler using fs.Sub(embeddedui.WebFS,"web") + http.FileServer(http.FS(sub)); ServeUI(addr string) error = http.ListenAndServe(addr, NewUIHandler()). cli/compose/compose.go: add --serve bool + --addr string (default ":8080") to the existing flagset; when --serve, call ServeUI(addr); otherwise keep the current resolution-preview behavior verbatim (do NOT regress compose_test.go). cmd/debateos/main.go: usage text gains the `compose --serve` note. scripts/build-ui-dual.sh: `set -euo pipefail`; run scripts/build-wasm.sh; build twice — `BASE_PATH=/debateos npm --prefix web run build` → copy web/build to dist/pages (Pages staging); `BASE_PATH= npm --prefix web run build` → rsync web/build into cli/embed/web (the go:embed target). If net/http does not auto-serve application/wasm for .wasm, add an explicit Content-Type override wrapper in NewUIHandler (Go 1.17+ usually auto-detects).
  </action>
  <verify>
    <automated>! go test ./cli/embed/... -run TestServeUI 2>/dev/null # RED before embed target exists</automated>
    <automated>go test ./cli/embed/... ./cli/compose/... -count=1</automated>
    <automated>go build ./cmd/debateos</automated>
    <automated>grep -q 'go:embed' cli/embed/embed.go && grep -q 'fs.Sub' cli/compose/serve.go && grep -q 'BASE_PATH=' scripts/build-ui-dual.sh && grep -q 'cli/embed/web' scripts/build-ui-dual.sh</automated>
  </verify>
  <done>ServeUI serves the embedded build at root with application/wasm for .wasm; `debateos compose --serve` added without regressing the preview path; build-ui-dual.sh produces both Pages (BASE_PATH=/debateos) and embed (BASE_PATH=) builds; embed compiles via placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Invariant-4 offline gate + coverage gate extension + Actions index workflow + deploy/brand docs + REQUIREMENTS finalize</name>
  <read_first>05-RESEARCH.md Pattern 7 (invariant4-check.sh: pkill forum, run compose/resolve, assert success) + Validation map; scripts/check-coverage.sh (existing two-gate pattern — add registry/ + forum/ gates at 85%); .github/workflows/reusable-build-speech.yml (existing workflow style); 05-CONTEXT.md §Hosting D15 (Oracle A1 arm64) + §Brand voice; 05-RESEARCH Open Questions RESOLVED (arm64 build command); .planning/REQUIREMENTS.md (section checkboxes + Traceability table)</read_first>
  <files>scripts/forum-offline-check.sh, scripts/check-coverage.sh, .github/workflows/registry-index.yml, forum/deploy/oracle-a1.md, docs/registry-forum-ui.md, .planning/REQUIREMENTS.md</files>
  <action>
    scripts/forum-offline-check.sh (invariant-4 gate, FORM-05): `set -euo pipefail`; ensure no forum process runs (`pkill -f forumctl 2>/dev/null || true`); run `go run ./cmd/debateos compose --dir examples/dual-foundation/` (resolves locally, zero Forum); run `go run ./cmd/resolve-json examples/dual-foundation` and assert non-empty resolved JSON (python3 json parse, Applied count); echo "INVARIANT-4 PASS: compose→resolve works with Forum offline". scripts/check-coverage.sh: add a ./registry/... gate and a ./forum/... gate at 85% each (mirror the existing check_package_coverage calls; keep resolver 90% + cli 85%). .github/workflows/registry-index.yml: on push to main affecting registry input → run the registry generator, emit index.json + browse HTML; the Pages deploy step is authored but gated/deferred per the prior-phase live-run policy (the build+generate step runs in CI). forum/deploy/oracle-a1.md: single static arm64 binary + one SQLite file — `GOOS=linux GOARCH=arm64 go build -o forumctl ./forum/cmd/forumctl`, systemd unit sketch, env vars GITHUB_CLIENT_ID/SECRET, reverse-proxy HTTPS termination, `forumctl reindex` command; live deploy deferred-to-host (D15). docs/registry-forum-ui.md: end-to-end doc — registry generation, dual-delivery (Pages + `debateos compose --serve`), Forum optional/rebuildable, GitHub OAuth app registration steps (deferred-live), with the BRAND-VOICE doc pass (debate vocabulary; A6 forbidden terms avoided). Update .planning/REQUIREMENTS.md: flip REG-01, UI-01, UI-02, BRND-01, FORM-01..05 to Complete in BOTH the section checkboxes (`- [x]`) and the Traceability table, each with a one-line evidence pointer (plan + test/script). Run the full suite as the milestone gate.
  </action>
  <verify>
    <automated>bash scripts/build-ui-dual.sh && go test ./... -count=1</automated>
    <automated>bash scripts/forum-offline-check.sh</automated>
    <automated>bash scripts/check-coverage.sh</automated>
    <automated>for id in REG-01 UI-01 UI-02 BRND-01 FORM-01 FORM-02 FORM-03 FORM-04 FORM-05; do grep "$id" .planning/REQUIREMENTS.md | grep -q Complete || { echo "MISSING $id Complete"; exit 1; }; done</automated>
  </verify>
  <done>Invariant-4 gate passes (compose→resolve works Forum-down); coverage gate extended to registry/+forum/ at 85% and passes; Actions index workflow authored (live deploy deferred); Oracle A1 + Pages deploy notes + brand-voice doc written; all 9 Phase-5 requirements flipped to Complete with evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| embedded build → CLI binary | Static UI assets compiled into the Go binary; must be the BASE_PATH= build |
| Forum process → core path | Core compose→resolve→build must never require the Forum (invariant 4) |
| deploy secrets → CI/host | OAuth + Pages secrets live in env/CI, never committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18 | Tampering | wrong base-path build embedded | mitigate | build-ui-dual.sh embeds the BASE_PATH= build into cli/embed/web; Pages gets the BASE_PATH=/debateos build (Pitfall 4) |
| T-05-19 | Denial of Service | Forum required for core path | mitigate | forum-offline-check.sh asserts compose→resolve works with no Forum process/socket (invariant 4 gate) |
| T-05-20 | Information Disclosure | deploy secrets in repo/artifacts | mitigate | GITHUB_CLIENT_ID/SECRET via env only (documented); no secrets committed; deploy live-run deferred-to-host |
</threat_model>

<verification>
- `bash scripts/build-ui-dual.sh && go test ./... -count=1` — full suite GREEN, dual build produced.
- `bash scripts/forum-offline-check.sh` — invariant-4 gate PASS.
- `bash scripts/check-coverage.sh` — resolver≥90, cli≥85, registry≥85, forum≥85.
- All 9 Phase-5 requirement IDs Complete in REQUIREMENTS.md.
</verification>

<success_criteria>
- UI-02: same build → GitHub Pages (BASE_PATH=/debateos) AND go:embed served at localhost root by `debateos compose --serve`.
- FORM-05: invariant-4 offline gate proves compose→resolve works Forum-down.
- REG-01: Actions index-rebuild-on-commit workflow authored (live run deferred-to-CI).
- D15: Oracle A1 + Pages deploy notes; BRND-01 doc-voice pass; coverage gate extended; v1.0 milestone requirements finalized.
</success_criteria>

<output>
Create `.planning/phases/05-registry-forum-debate-ui/05-06-SUMMARY.md` when done.
</output>
