---
phase: 05-registry-forum-debate-ui
plan: 04
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - web/src/routes/debate/+page.svelte
  - web/src/routes/debate/+page.ts
  - web/src/routes/export/+page.svelte
  - web/src/lib/components/DebateStage.svelte
  - web/src/lib/components/FoundationBar.svelte
  - web/src/lib/components/PaneCard.svelte
  - web/src/lib/components/ConflictOverlay.svelte
  - web/src/lib/components/ConflictBadge.svelte
  - web/src/lib/components/ResolutionPanel.svelte
  - web/src/lib/components/ExplanationCard.svelte
  - web/src/lib/components/WasmLoadGate.svelte
  - web/src/lib/stores/speech.ts
  - web/src/lib/conflict.ts
  - web/src/lib/conflict.test.ts
  - web/tests/wasm-render.spec.ts
  - web/tests/a11y.spec.ts
  - web/tests/fixtures/conflicting-speech.json
autonomous: true
requirements: [UI-01, UI-02, BRND-01]
must_haves:
  truths:
    - "The /debate screen loads the WASM resolver client-side (ssr=false) and calls debateosResolve on pane add/remove"
    - "ConflictOverlay renders triple encoding (color + icon + text label) per UI-SPEC assertion A1"
    - "Explanation.Text from the resolver is rendered verbatim in ExplanationCard (assertion A9)"
    - "The compose path works with the Forum unreachable (assertion A3 — WASM-only, no Forum call)"
    - "All UI copy uses canonical debate vocabulary; no config/preset/distro (assertion A6 / BRND-01)"
  artifacts:
    - path: "web/src/lib/components/ConflictOverlay.svelte"
      provides: "Triple-encoded conflict visualization (A1)"
    - path: "web/src/lib/components/ExplanationCard.svelte"
      provides: "Verbatim Explanation.Text rendering (A9)"
    - path: "web/src/lib/conflict.ts"
      provides: "Pure mapping ResolvedSpeech.Explanations → conflict view state"
    - path: "web/tests/wasm-render.spec.ts"
      provides: "Playwright WASM-render e2e (A3 + A9)"
  key_links:
    - from: "web/src/routes/debate/+page.svelte"
      to: "$lib/wasm debateosResolve"
      via: "onMount + debounced resolve call"
      pattern: "debateosResolve"
    - from: "web/src/routes/debate/+page.ts"
      to: "ssr=false"
      via: "page option (WASM can't run in SSR)"
      pattern: "ssr = false"
---

<objective>
Build the visual Debate UI compose flow (UI-01) and its primary gate, the Playwright WASM-render e2e: the /debate screen (ssr=false) loads debateos.wasm, lets a user add points into panes, calls debateosResolve on every change (150ms debounce), and renders live conflict visualization (ConflictOverlay triple encoding + ResolutionPanel ExplanationCards) plus the export/build-handoff screen. The UI calls WASM and renders Explanation output — it NEVER reimplements resolution (invariant 3). Brand voice (BRND-01) applied verbatim from UI-SPEC.

Purpose: UI-01 live conflict visualization powered client-side by the WASM resolver; the Playwright test is the primary UI-02 render gate (Chromium confirmed working on this host).
Output: Debate components + conflict-mapping logic + export screen + Playwright e2e (A1/A3/A6/A9 + a11y A2/A7).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-registry-forum-debate-ui/05-CONTEXT.md
@.planning/phases/05-registry-forum-debate-ui/05-RESEARCH.md
@.planning/phases/05-registry-forum-debate-ui/05-UI-SPEC.md

# Contracts (read first)
@resolver/resolve/explanation.go
@resolver/wasm/main.go
# 05-02 created: web/src/lib/wasm.ts, types.ts, app.css @theme tokens, build-wasm.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Debate components + conflict-mapping logic + export screen</name>
  <read_first>05-UI-SPEC.md §Core Debate Components, §Conflict Visualization Spec (overlay states keyed on Explanation.Rule: rule2=hard, rule1=will-be-dropped, hardware-skip=hardware mismatch, no-conflict=compatible, PatchOffered=patch available), §Explanation rendering (ExplanationCard fields), §Copywriting contract (verbatim headings/bodies); resolver/resolve/explanation.go (Explanation.Rule values + field JSON tags); 05-02 wasm.ts/types.ts (debateosResolve, parseResolveOutput)</read_first>
  <files>web/src/lib/conflict.ts, web/src/lib/conflict.test.ts, web/src/lib/stores/speech.ts, web/src/lib/components/DebateStage.svelte, web/src/lib/components/FoundationBar.svelte, web/src/lib/components/PaneCard.svelte, web/src/lib/components/ConflictOverlay.svelte, web/src/lib/components/ConflictBadge.svelte, web/src/lib/components/ResolutionPanel.svelte, web/src/lib/components/ExplanationCard.svelte, web/src/lib/components/WasmLoadGate.svelte, web/src/routes/debate/+page.svelte, web/src/routes/debate/+page.ts, web/src/routes/export/+page.svelte</files>
  <behavior>
    - Vitest TestMapExplanation: conflict.ts mapExplanation(e) maps Explanation{rule:"rule2"} → {state:"hard", icon:"AlertTriangle", label:"Hard conflict"}; rule:"rule1" → {state:"warn", icon:"Info", label:"Will be dropped"}; rule:"hardware-skip" → {icon:"Cpu", label:"Hardware mismatch"}; rule:"no-conflict" → {state:"compat", icon:"CheckCircle2", label:"Compatible"}.
    - Vitest TestPatchBadge: an Explanation with non-empty patch_offered yields an additional {state:"patch", icon:"Puzzle", label:"Patch available"} alongside (not replacing) the base state.
    - Vitest TestExplanationVerbatim: the view model carries Explanation.text UNCHANGED (no substitution) — guards A9 at the data layer.
    - RED FIRST: `! (cd web && npx vitest run src/lib/conflict.test.ts)` fails before conflict.ts exists.
  </behavior>
  <action>
    conflict.ts: pure mapExplanation(e: Explanation) → ConflictView {state,icon,label,text,opinionsInvolved,kept,dropped,patchOffered} per the UI-SPEC state table — text MUST be e.text verbatim (A9). stores/speech.ts: a Svelte store holding {foundation, panes: PaneCard[], opinions, hardware} + an addPoint/removePane API. debate/+page.ts: `export const ssr = false` (WASM can't SSR — Pitfall 3) + prerender stays via layout. debate/+page.svelte: onMount loadDebateosWasm() behind WasmLoadGate (role=status "Loading the resolver…"); on speech-store change, 150ms debounce → debateosResolve(buildResolveInput(...)) → map result.explanations via conflict.ts → drive ConflictOverlay + ResolutionPanel. Components implement UI-SPEC exactly: FoundationBar (56px), PaneCard (role=region, 44px touch header — A2), ConflictOverlay (absolute region, role=status, aria-label "[State]: A vs B", renders ConflictBadge = color fill + lucide icon + visible text label = TRIPLE encoding A1; add data-conflict-state attr so the test can assert), ExplanationCard (Rule pill + verbatim Explanation.text + opinion chips + kept/dropped rows + patch row + trust-warning banner), ResolutionPanel + BuildReadyBanner (shown when no hard conflicts). Use accent-brand ONLY on primary CTAs / active-pane ring / focus rings (A5). export/+page.svelte: resolved speech YAML preview + `debateos build` command + download, build-stage rhetoric names (UI-SPEC table). ALL copy verbatim from UI-SPEC §Copywriting contract; canonical vocabulary only (A6).
  </action>
  <verify>
    <automated>! (cd web && npx vitest run src/lib/conflict.test.ts) 2>/dev/null # RED before impl</automated>
    <automated>cd web && npx vitest run src/lib/conflict.test.ts</automated>
    <automated>grep -q 'ssr = false' web/src/routes/debate/+page.ts</automated>
    <automated>cd web && npm run build</automated>
    <automated>grep -q 'data-conflict-state' web/src/lib/components/ConflictOverlay.svelte</automated>
  </verify>
  <done>conflict.ts maps every Explanation.Rule to the correct triple-encoded view with text verbatim; /debate is ssr=false and calls debateosResolve via debounce; components match UI-SPEC; build succeeds; export screen hands off to `debateos build`.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright WASM-render e2e + accessibility/brand assertions (A1,A2,A3,A6,A7,A9)</name>
  <read_first>05-RESEARCH.md Pattern 7 (Playwright invariant-4/WASM e2e: page.route abort Forum, page.evaluate window.debateosResolve, assert ResolvedSpeech), §Validation Architecture test map; 05-UI-SPEC.md §Verifiable Design Assertions A1/A2/A3/A6/A7/A9; 05-02 playwright.config.ts (chromium, baseURL)</read_first>
  <files>web/tests/wasm-render.spec.ts, web/tests/a11y.spec.ts, web/tests/fixtures/conflicting-speech.json</files>
  <action>
    Author a fixture conflicting-speech.json = {speech, opinions, hardware} that the resolver returns at least one rule2 hard conflict for (reuse examples/conflicting shape — translate the YAML conflicting example to the WASM JSON input). build-wasm.sh must run before the e2e (Playwright serves web/build/ which needs static/debateos.wasm). wasm-render.spec.ts: (A3) block all Forum origins (page.route('**/api/**'|'**/health**', abort)), goto /debate, wait for WASM ready, inject the fixture via page.evaluate(window.debateosResolve), assert the returned ResolvedSpeech has explanations and (for a clean fixture) Applied.length>0 — proves compose works Forum-offline. (A9) compare the rendered ExplanationCard text content to the resolver's Explanation[0].text byte-for-byte. (A1) assert a [data-conflict-state="hard"] element has a red computed background, a child lucide AlertTriangle icon, AND visible text matching /hard conflict/i. a11y.spec.ts: (A2) every .pane-header / conflict-action button offsetHeight>=44; (A7) no font-size outside {13,14,16,20,28}px and no font-weight outside {400,600} in computed styles; (A6) page.getByText() finds NONE of config/preset/distro/"package set" in visible text on /, /debate, /export. Add the e2e + unit run to web/package.json scripts (test:unit = vitest run; test:e2e = playwright test).
  </action>
  <verify>
    <automated>bash scripts/build-wasm.sh</automated>
    <automated>cd web && npm run build && npx playwright test tests/wasm-render.spec.ts</automated>
    <automated>cd web && npx playwright test tests/a11y.spec.ts</automated>
  </verify>
  <done>Playwright e2e GREEN headless: A3 (WASM compose works with Forum blocked), A9 (verbatim Explanation text), A1 (triple encoding), A2 (44px touch), A6 (no forbidden terms), A7 (typography scale).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → WASM | User-composed speech crosses into client-side resolver |
| Forum (optional) → UI | UI must never hard-depend on Forum for compose (invariant 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | client-side resolution drift | mitigate | All resolution via window.debateosResolve; conflict.ts only maps output, never decides (invariant 3); A9 asserts verbatim text |
| T-05-11 | Denial of Service | rapid resolve calls | accept | 150ms debounce; WASM is synchronous and fast (<16ms typical per RESEARCH) |
| T-05-12 | Spoofing | accessibility (color-only conflict cue) | mitigate | Triple encoding (color+icon+text) enforced by A1; amber for warnings (deuteranopia-safe) |
</threat_model>

<verification>
- `cd web && npx vitest run` GREEN.
- `cd web && npm run build && npx playwright test` GREEN headless.
- A3 confirms the compose path needs zero Forum reachability (feeds the invariant-4 gate in 05-06).
</verification>

<success_criteria>
- UI-01: live conflict visualization driven by client-side WASM, Explanation rendered verbatim.
- UI-02 render gate: Playwright WASM-render e2e passes headless.
- BRND-01: A6 brand-vocabulary assertion passes.
</success_criteria>

<output>
Create `.planning/phases/05-registry-forum-debate-ui/05-04-SUMMARY.md` when done.
</output>
