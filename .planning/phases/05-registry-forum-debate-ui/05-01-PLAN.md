---
phase: 05-registry-forum-debate-ui
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - registry/generator.go
  - registry/generator_test.go
  - registry/index/index.go
  - registry/index/compat.go
  - registry/index/compat_test.go
  - registry/testdata/fixtures/points/sample-point.yaml
  - registry/testdata/fixtures/opinions/sample-opinions.yaml
  - registry/testdata/golden/index.json
autonomous: true
requirements: [REG-01]
must_haves:
  truths:
    - "Registry generator reads point/speech YAML from fixture dirs and validates each via resolver/parse"
    - "Generator emits a deterministic JSON index (byte-identical across two runs on identical input)"
    - "Each point entry carries foundation-compatibility computed from translators/*/capabilities.json (not duplicated)"
    - "Generator emits minimal static browse HTML consumable by GitHub Pages"
    - "All phase Go deps (chi, modernc.org/sqlite v1.46.1, x/oauth2) are present in go.mod after this plan"
  artifacts:
    - path: "registry/generator.go"
      provides: "Index generation entrypoint (GenerateIndex) + HTML emit"
      min_lines: 60
    - path: "registry/index/index.go"
      provides: "RegistryIndex, PointEntry structs + deterministic marshaling"
    - path: "registry/index/compat.go"
      provides: "ComputeCompat from capabilities.json"
      exports: ["ComputeCompat", "FoundationCompat"]
    - path: "go.mod"
      provides: "All phase Go dependencies (single-owner)"
      contains: "modernc.org/sqlite v1.46.1"
  key_links:
    - from: "registry/generator.go"
      to: "resolver/parse"
      via: "ParsePoint/ParseOpinion validation"
      pattern: "parse\\.Parse(Point|Opinion|Speech)"
    - from: "registry/index/compat.go"
      to: "translators/*/capabilities.json"
      via: "capability-set membership check"
      pattern: "capabilities"
---

<objective>
Build the Go static registry index generator (REG-01): scan point/speech YAML from fixture dirs, validate each via resolver/parse, compute foundation-compatibility from the translators' capabilities.json, and emit a deterministic JSON index plus minimal static browse HTML for GitHub Pages. Git stays authoritative — the index is a derived, idempotent cache (per D12).

This plan is also the SINGLE OWNER of go.mod/go.sum for Phase 5: it adds every Go dependency the phase needs (chi, modernc.org/sqlite v1.46.1 PINNED, golang.org/x/oauth2) so later Forum plans never touch go.mod and can run in parallel.

Purpose: Provide the authoritative discovery substrate (REG-01) that both the Debate UI (point discovery) and the Forum (what it indexes) consume.
Output: registry/ package + go.mod with all phase deps + golden index + deterministic generator.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-registry-forum-debate-ui/05-CONTEXT.md
@.planning/phases/05-registry-forum-debate-ui/05-RESEARCH.md

# Contracts this plan consumes (read first)
@resolver/types.go
@resolver/parse/parse.go
@cli/internal/loader/loader.go
@translators/arch/capabilities.json
@translators/debian/capabilities.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add all phase Go deps (single-owner go.mod) + foundation-compat with RED tests</name>
  <read_first>resolver/types.go (Point, PointMember, Opinion, TranslatorCapabilities []string lines 160-215); translators/arch/capabilities.json + translators/debian/capabilities.json ({"capabilities": [...]} shape)</read_first>
  <files>go.mod, go.sum, registry/index/index.go, registry/index/compat.go, registry/index/compat_test.go, registry/testdata/fixtures/opinions/sample-opinions.yaml, registry/testdata/fixtures/points/sample-point.yaml</files>
  <behavior>
    - Test ComputeCompat: a point whose member opinions require only tokens present in BOTH arch and debian capability sets → []FoundationCompat with both {Foundation:"arch",Compatible:true} and {Foundation:"debian",Compatible:true}.
    - Test ComputeCompat: a point requiring a token present ONLY in arch (e.g. "deploy-sddm-theme") → arch Compatible:true, debian Compatible:false with Missing containing that token.
    - Test ComputeCompat determinism: FoundationCompat slice is sorted by Foundation; Missing sorted lexically.
    - RED FIRST: `! go test ./registry/index/... -run TestComputeCompat` must fail (no impl yet).
  </behavior>
  <action>
    First add every Phase-5 Go dependency in ONE go.mod edit so no later plan touches go.mod: run `go get github.com/go-chi/chi/v5@v5.3.0 modernc.org/sqlite@v1.46.1 golang.org/x/oauth2@v0.36.0` then `go mod tidy`. modernc.org/sqlite is PINNED to v1.46.1 — do NOT use v1.47+ (requires Go 1.25; would auto-download a toolchain). Verify `go 1.24.0` directive is unchanged. These deps are not yet imported by registry/, so add a tiny `import _` blank-import guard is NOT needed — `go mod tidy` will keep them as indirect until Forum plans import them; to keep them as direct requires, add them to a `tools`-style guard file `registry/deps_guard.go` (package registry) with blank imports `_ "github.com/go-chi/chi/v5"`, `_ "modernc.org/sqlite"`, `_ "golang.org/x/oauth2"` and a comment "phase-5 dep anchor; consumed by forum/ (05-03, 05-05)". Then define index.FoundationCompat{Foundation string; Compatible bool; Missing []string `json:"missing_capabilities,omitempty"`} and ComputeCompat(point resolver.Point, opinions map[resolver.OpinionID]resolver.Opinion, caps map[string][]string) []FoundationCompat in registry/index/compat.go. Algorithm (per RESEARCH Pattern 6): collect the union of TranslatorCapabilities across the point's member opinions; for each foundation in caps, a token is satisfied iff it is in that foundation's set; Compatible = all required tokens satisfied; Missing = sorted unsatisfied tokens. Sort the returned slice by Foundation. Author registry/testdata fixtures (one point with 2-3 member opinions; opinions carry realistic translator_capabilities tokens drawn verbatim from the two capabilities.json files — pick at least one arch-only token to force a debian-incompatible case).
  </action>
  <verify>
    <automated>! go test ./registry/index/... -run TestComputeCompat 2>/dev/null # RED before impl; then after impl: go test ./registry/index/... -run TestComputeCompat -count=1</automated>
    <automated>grep -q 'modernc.org/sqlite v1.46.1' go.mod && grep -q 'go-chi/chi/v5' go.mod && grep -q 'golang.org/x/oauth2' go.mod</automated>
    <automated>grep -q '^go 1.24' go.mod</automated>
  </verify>
  <done>go.mod contains all three phase deps with sqlite pinned to v1.46.1 and go directive still 1.24.x; ComputeCompat tests pass GREEN after impl; arch-only-token point is debian-incompatible with the token in Missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generator — parse+validate fixtures, emit deterministic index.json + browse HTML</name>
  <read_first>resolver/parse/parse.go (ParsePoint/ParseOpinion/ParseSpeech signatures); cli/internal/loader/loader.go (loadPoints/loadOpinions dir-walk pattern, lines 78-138); scripts/determinism-test.sh (existing determinism gate pattern, BLD-03)</read_first>
  <files>registry/generator.go, registry/generator_test.go, registry/index/index.go, registry/testdata/golden/index.json</files>
  <behavior>
    - Test TestGenerateIndex: GenerateIndex over registry/testdata/fixtures produces a RegistryIndex with the fixture point present, with foundation_compat populated, and members listed sorted by ID.
    - Test TestGenerateIndex validation: a malformed opinion YAML in the fixture dir causes GenerateIndex to return a non-nil error naming the offending file (every doc validated via resolver/parse — no silent skip).
    - Test TestDeterminism: marshaling the same RegistryIndex twice yields byte-identical JSON; generated_at is injected (not time.Now() at marshal) so it is stable in the golden — pass a fixed timestamp into GenerateIndex.
    - Test golden compare: emitted JSON equals registry/testdata/golden/index.json byte-for-byte.
    - RED FIRST: `! go test ./registry/... -run TestGenerateIndex` fails before impl.
  </behavior>
  <action>
    Define index.RegistryIndex{Schema int `json:"schema"`; GeneratedAt string `json:"generated_at"`; Points []PointEntry `json:"points"`} and index.PointEntry{ID, Name, Intent, Curator string; Members []string; FoundationCompat []FoundationCompat `json:"foundation_compat"`; CommitDate string `json:"commit_date,omitempty"`; Tags []string `json:"tags"`} in registry/index/index.go (schema version constant = 1). Implement GenerateIndex(fixturesDir string, caps map[string][]string, generatedAt string) (*index.RegistryIndex, error) in registry/generator.go: walk fixturesDir/points and fixturesDir/opinions, ParsePoint/ParseOpinion each (return wrapped error with filename on parse failure — this is the "validate every doc via resolver/parse" guarantee for REG-01), build an opinions map, compute FoundationCompat per point via index.ComputeCompat, sort Points by ID and Members by ID for determinism. Add LoadCapabilities(archPath, debianPath string) (map[string][]string, error) reading the two capabilities.json files into map[foundation][]token (keys "arch","debian"). Add EmitHTML(idx *index.RegistryIndex, w io.Writer) writing a minimal static browse page (one row per point: name, curator, compat badges as plain text "arch ✓ / debian ✗") — no JS, Pages-static. Marshal index with encoding/json MarshalIndent (2-space) for the golden; struct field order gives deterministic key order. Author registry/testdata/golden/index.json by running the generator once with a fixed generatedAt and committing the output verbatim.
  </action>
  <verify>
    <automated>! go test ./registry/... -run TestGenerateIndex 2>/dev/null # RED before impl</automated>
    <automated>go test ./registry/... -run 'TestGenerateIndex|TestDeterminism' -count=1</automated>
    <automated>go vet ./registry/...</automated>
  </verify>
  <done>GenerateIndex validates every fixture via resolver/parse, errors with filename on malformed input, emits index matching the committed golden byte-for-byte across two runs; EmitHTML produces a JS-free browse page; all registry tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture YAML → generator | Untrusted point/speech YAML from (eventually) third-party GitHub repos crosses into the generator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | registry generator YAML ingest | mitigate | Every doc validated via resolver/parse (KnownFields strict); malformed input → hard error naming the file, never silently indexed |
| T-05-02 | Information Disclosure | index.json output | accept | Index contains only already-public point metadata; no secrets, no private pane data ever read |
| T-05-SC | Tampering | go get of chi/sqlite/oauth2 | mitigate | All three packages verified in RESEARCH Package Legitimacy Audit (all OK/Approved, no ASSUMED/SUS); sqlite pinned to exact v1.46.1 |
</threat_model>

<verification>
- `go test ./registry/... -count=1` all GREEN.
- `go test ./... -count=1` — resolver/cli/translators stay green (no regression from go.mod changes).
- go.mod single-owner invariant: this is the only Phase-5 plan modifying go.mod.
</verification>

<success_criteria>
- REG-01 generator produces a deterministic JSON index validated via resolver/parse with foundation-compat from capabilities.json + a static browse HTML page.
- go.mod carries all phase Go deps (chi, modernc.org/sqlite v1.46.1 pinned, x/oauth2) with go 1.24 intact.
</success_criteria>

<output>
Create `.planning/phases/05-registry-forum-debate-ui/05-01-SUMMARY.md` when done.
</output>
